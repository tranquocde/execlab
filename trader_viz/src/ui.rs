pub const PAGE: &str = r####"<!doctype html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="icon" type="image/png" href="/icon.png">
<title>TradeLens</title>
<style>
:root{--ink:#161820;--muted:#70727b;--line:#b8bac2;--paper:#fafafa;--panel:#fff;--purple:#6d3bc1;--green:#16813b;--red:#ba2c36;--blue:#2563a8;--orange:#d36b22}
*{box-sizing:border-box}body{margin:0;background:#eee;color:var(--ink);font:14px/1.45 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}.shell{max-width:1880px;margin:auto;min-height:100vh;background:var(--paper);padding:20px 28px}.brand{display:flex;align-items:center;justify-content:space-between;border-bottom:2px solid var(--ink);padding-bottom:12px;margin-bottom:18px}.brand b{font-size:32px;line-height:1.15}.vps{color:var(--purple);font-size:20px;font-weight:900}.title{font-size:22px;font-weight:800;margin:6px 0}.sub{color:var(--muted);margin-bottom:18px}.order-context{display:flex;align-items:stretch;gap:0;margin:9px 0 16px;border:2px solid var(--purple);background:#f7f3ff;width:max-content;max-width:100%;box-shadow:3px 3px 0 rgba(109,59,193,.18)}.order-context span{display:grid;gap:2px;padding:8px 16px;border-right:1px dashed var(--purple);min-width:120px}.order-context span:last-child{border-right:0}.order-context small{color:var(--purple);font-size:11px;font-weight:800;letter-spacing:.08em}.order-context strong{font-size:20px;line-height:1.1}.order-context .side-buy strong{color:var(--blue)}.order-context .side-sell strong{color:var(--orange)}.panel{background:var(--panel);border:1px solid var(--ink);padding:16px;margin-bottom:14px}.dashed{border-style:dashed}.row{display:flex;gap:14px;align-items:center;flex-wrap:wrap}.spread{justify-content:space-between}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:18px}.perf{display:grid;grid-template-columns:minmax(320px,var(--strategy-width,36%)) 10px minmax(500px,1fr);gap:0}.perf>section:first-child{padding-right:8px;min-width:0}.perf>section:last-child{padding-left:8px;min-width:0}.perf-divider{position:relative;cursor:col-resize;touch-action:none;background:transparent}.perf-divider::before{content:'';position:absolute;top:0;bottom:0;left:4px;width:2px;background:var(--ink);transition:width .12s,background .12s}.perf-divider:hover::before,.perf-divider.dragging::before{left:2px;width:6px;background:var(--purple)}.perf-divider::after{content:'⋮';position:sticky;display:block;top:45vh;color:#fff;background:var(--purple);text-align:center;font-weight:900;line-height:34px;height:34px;width:10px}.stack{display:grid;gap:12px}label{font-weight:700;display:grid;gap:6px}input,select,textarea,button{font:inherit}input,select,textarea{background:white;border:1px solid var(--ink);padding:10px;min-width:160px}textarea{min-height:90px}button{background:#fff;border:1px solid var(--ink);padding:9px 14px;cursor:pointer}button:hover{background:#f0edf8}.primary{background:var(--purple);color:white;border-color:var(--purple);font-weight:800}.danger{color:var(--red)}.good{color:var(--green);font-weight:800}.bad{color:var(--red);font-weight:800}.muted{color:var(--muted)}.tag{border:1px solid var(--line);padding:2px 7px}.selected{outline:2px solid var(--purple);outline-offset:-2px;background:#4f258f;color:#fff;font-weight:800}.selected td{border-bottom-color:#bea7ea}.selected .good{color:#8ff0ad}.selected .bad{color:#ffb0b7}.selected.active-strategy{box-shadow:inset 7px 0 0 #f4c542,inset 0 0 0 2px #f4c542}table{width:100%;border-collapse:collapse;white-space:nowrap}th,td{padding:8px 9px;border-bottom:1px dashed var(--line);text-align:right}th{position:sticky;top:0;background:#fff;z-index:1;cursor:pointer}th:first-child,td:first-child{text-align:left}.scroll{overflow:auto;max-width:100%}.scenario{display:grid;grid-template-columns:1fr auto;gap:12px;border-bottom:1px dashed var(--line);padding:14px 2px}.scenario:last-child{border:0}.scenario h3{margin:0 0 8px}.steps{display:flex;gap:8px;margin-bottom:16px}.step{border-bottom:3px solid var(--line);padding:6px 12px}.step.on{border-color:var(--purple);font-weight:800;color:var(--purple)}.metric{border:1px dashed var(--line);padding:10px}.metric b{font-size:18px}.progress{height:20px;border:1px solid var(--ink);background:#fff}.bar{height:100%;background:var(--purple);transition:width .3s}canvas{display:block;width:100%;height:300px;border:1px solid var(--ink);background:#fff;cursor:grab;touch-action:none}canvas.dragging{cursor:grabbing}.chart-head{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.chart-title{font-weight:800;margin-bottom:3px}.chart-note{color:var(--muted);font-size:12px}.chart-controls{display:flex;gap:5px;align-items:center}.chart-controls button{padding:3px 9px}.legend{display:flex;gap:12px;flex-wrap:wrap;margin:8px 0;font-size:12px}.legend-item{display:inline-flex;gap:5px;align-items:center}.legend-dot{width:10px;height:10px;display:inline-block}.replay{margin-top:16px;border-top:3px solid var(--purple);padding-top:16px}.replay-fill{margin-bottom:16px}.replay-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.hidden{display:none!important}.error{background:#fff0f0;border:1px solid var(--red);color:var(--red);padding:10px;margin:10px 0}.empty{padding:36px;text-align:center;color:var(--muted)}@media(max-width:1050px){.perf,.grid2,.replay-grid{grid-template-columns:1fr}.perf>section:first-child,.perf>section:last-child{padding:0}.perf-divider{display:none}.shell{padding:14px}.vps{font-size:16px}}@media(max-width:620px){.order-context{width:100%;flex-direction:column}.order-context span{border-right:0;border-bottom:1px dashed var(--purple)}.order-context span:last-child{border-bottom:0}}
.run-stats{display:grid;grid-template-columns:repeat(5,minmax(100px,1fr));gap:10px;margin:14px 0}.run-stat{border:1px dashed var(--line);padding:9px}.run-stat small{display:block;color:var(--muted);font-weight:800}.run-stat strong{font-size:20px}.run-timing{display:flex;gap:24px;flex-wrap:wrap;margin-top:10px}@media(max-width:760px){.run-stats{grid-template-columns:repeat(2,1fr)}}
.run-mode-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px;max-width:900px}.run-mode{padding:24px;text-align:left;border:2px solid var(--ink);box-shadow:4px 4px 0 #ddd}.run-mode:hover{border-color:var(--purple);box-shadow:6px 6px 0 #d8cbed}.run-mode b{display:block;font-size:20px;margin-bottom:8px}.run-mode span{color:var(--muted)}@media(max-width:620px){.run-mode-grid{grid-template-columns:1fr}}
.run-log-panel{background:#15131a;color:#e8e4ef}.run-log-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px}.run-log{height:300px;overflow:auto;margin:0;padding:12px;border:1px solid #4b4653;background:#09080c;color:#d9f5df;white-space:pre-wrap;overflow-wrap:anywhere;font:12px/1.45 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}
.scenario-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:18px;margin-top:16px}.scenario-grid>.empty{grid-column:1/-1}.scenario-grid .scenario{display:flex;flex-direction:column;gap:13px;aspect-ratio:1/1;min-height:320px;padding:17px;border:2px solid var(--ink);background:#fff;box-shadow:5px 5px 0 #ded8e9;transition:transform .12s,box-shadow .12s}.scenario-grid .scenario:hover{transform:translate(-2px,-2px);box-shadow:7px 7px 0 #cbbce5}.scenario-grid .scenario h3{font-size:19px;line-height:1.25;margin:0;overflow-wrap:anywhere}.scenario-top{display:flex;justify-content:space-between;align-items:flex-start;gap:10px}.scenario-status{font-size:11px;font-weight:900;padding:3px 7px;border:1px solid currentColor;white-space:nowrap}.scenario-status.ready{color:var(--green)}.scenario-status.new{color:var(--muted)}.scenario-description{color:var(--muted);min-height:40px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.scenario-facts{display:grid;grid-template-columns:1fr 1fr;border-block:1px dashed var(--line)}.scenario-fact{padding:9px 6px}.scenario-fact:nth-child(odd){border-right:1px dashed var(--line)}.scenario-fact small{display:block;color:var(--muted);font-size:10px;font-weight:900;letter-spacing:.06em}.scenario-fact strong{display:block;font-size:17px;margin-top:2px}.scenario-latest{font-size:12px}.scenario-actions{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-top:auto}.scenario-actions button{min-width:0;padding:7px 5px;font-size:12px}.scenario-actions .scenario-run,.scenario-actions .scenario-view{grid-column:span 3}.scenario-actions .scenario-view{border-color:var(--purple);color:var(--purple);font-weight:800}@media(max-width:680px){.scenario-grid{grid-template-columns:1fr}.scenario-grid .scenario{aspect-ratio:auto;min-height:330px}}
.replay-table-box{border:1px solid var(--ink);padding:13px;margin:14px 0;min-width:0}.replay-table-head{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap}.replay-table-head h3{margin:0}.pager{display:flex;align-items:center;gap:6px;flex-wrap:wrap}.pager select,.pager input,.pager button{padding:3px 7px;min-width:0}.pager input{width:60px;text-align:right}.replay-table{width:max-content;min-width:100%}.replay-table th,.replay-table td{white-space:nowrap;text-align:right}.replay-table th:first-child,.replay-table td:first-child{text-align:left}.replay-table th:not(:last-child),.replay-table td:not(:last-child){border-right:1px dashed #d4d4d4}.replay-table th.sort-asc::after{content:' ▲'}.replay-table th.sort-desc::after{content:' ▼'}
.ob-controls{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin:12px 0}.ob-controls button,.ob-controls select{padding:4px 10px}.ob-controls input{flex:1;min-width:220px}.ob-time{font-weight:800;color:var(--purple)}.ob-books{display:grid;grid-template-columns:1fr 1fr;gap:18px}.ob-book{border:1px solid var(--ink);padding:10px}.ob-book h3{margin:0 0 8px}.ob-head,.ob-row{display:grid;grid-template-columns:minmax(80px,1fr) 100px 100px minmax(80px,1fr);gap:6px;align-items:center;text-align:center}.ob-head{font-weight:800;border-bottom:1px dashed var(--line);padding-bottom:5px}.ob-row{height:32px}.ob-bid-label,.ob-bid-price{color:#0072b2}.ob-ask-label,.ob-ask-price{color:#d55e00}.ob-bid-price,.ob-ask-price{font-weight:800;padding:3px 5px;border:1px dashed currentColor}.ob-side{position:relative;height:24px;background:#eee;overflow:hidden}.ob-bar{position:absolute;inset-block:0;opacity:.72}.ob-bid .ob-bar{right:0;background:#0072b2}.ob-ask .ob-bar{left:0;background:#d55e00}.ob-qty{position:relative;z-index:1;line-height:24px;padding:0 5px}.ob-bid .ob-qty{text-align:right}.ob-ask .ob-qty{text-align:left}.ob-empty{color:#999}.ob-legend{font-size:12px;color:var(--muted);margin-top:8px}@media(max-width:760px){.ob-books{grid-template-columns:1fr}}
.ob-controls select{height:28px;min-width:72px;width:72px;padding:1px 5px}
.scenario-actions{grid-template-columns:repeat(4,1fr)}.scenario-actions .scenario-run,.scenario-actions .scenario-view{grid-column:span 4}
.scenario-grid{grid-template-columns:repeat(auto-fill,minmax(370px,1fr))}.scenario-grid .scenario{min-height:370px;gap:10px}.scenario-grid .scenario>.row{max-height:52px;overflow:hidden;align-content:flex-start}.scenario-grid .scenario-actions{gap:5px}.scenario-grid .scenario-actions button{padding:6px 4px}@media(max-width:820px){.scenario-grid{grid-template-columns:1fr}.scenario-grid .scenario{aspect-ratio:auto;min-height:370px}}
.scenario-grid{grid-template-columns:repeat(auto-fill,minmax(390px,1fr));align-items:stretch}.scenario-grid .scenario{aspect-ratio:auto;min-height:430px;height:auto;overflow:visible}.scenario-grid .scenario>.row{max-height:none;overflow:visible}.scenario-grid .scenario-actions{grid-template-columns:repeat(2,minmax(0,1fr))}.scenario-grid .scenario-actions .scenario-run,.scenario-grid .scenario-actions .scenario-view{grid-column:span 2}.scenario-grid .scenario-actions button{white-space:normal;line-height:1.2;min-height:34px}@media(max-width:480px){.scenario-grid{grid-template-columns:minmax(0,1fr)}.scenario-grid .scenario{min-height:0}}
.scenario-symbol{font-weight:900;font-size:15px;border-width:2px}.scenario-side{font-weight:900;border-width:2px}.scenario-side.side-sell{color:var(--red);background:#fff0f0}.scenario-side.side-buy{color:var(--green);background:#effaf2}.scenario-side-text.side-sell{color:var(--red)}.scenario-side-text.side-buy{color:var(--green)}
.scenario-best{border:2px solid var(--purple);background:#f7f3ff;padding:9px}.scenario-best-title{color:var(--purple);font-size:11px;font-weight:900;letter-spacing:.07em}.scenario-best-params{font-weight:900;margin:3px 0 6px}.scenario-best-metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px}.scenario-best-metrics span{border-top:1px dashed var(--purple);padding-top:5px;min-width:0;overflow-wrap:anywhere}.scenario-best-metrics small{display:block;color:var(--muted);font-size:9px;font-weight:800;line-height:1.15;min-height:21px}.scenario-best-empty{color:var(--muted);font-size:12px}@media(max-width:520px){.scenario-best-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}}
.tradelens-name{display:flex;align-items:center;gap:9px}.tradelens-icon{width:56px;height:56px;object-fit:contain;flex:none}
.brand-copy{display:flex;flex-direction:column;align-items:flex-start}.language-switch{display:inline-grid;grid-template-columns:auto 44px auto;align-items:center;gap:7px;margin-top:8px;border:1px solid var(--line);padding:5px 8px;background:#fff;font-size:13px;font-weight:900}.language-switch:hover{background:#f0edf8}.language-switch-label{display:inline-flex;align-items:center;gap:4px;color:var(--muted);transition:color .15s}.language-switch[data-language="en"] .language-en,.language-switch[data-language="vi"] .language-vi{color:var(--purple)}.language-track{position:relative;width:44px;height:22px;border:1px solid var(--ink);border-radius:14px;background:#ddd}.language-knob{position:absolute;top:2px;left:2px;width:16px;height:16px;border-radius:50%;background:var(--purple);transition:transform .16s ease}.language-switch[data-language="vi"] .language-knob{transform:translateX(22px)}
.guide-toc{margin:14px 0;border:1px dashed var(--line);padding:10px}.guide-toc b{display:block;margin-bottom:6px}.guide-toc a{display:block;color:var(--purple);font-weight:800;text-decoration:none;padding:4px 2px}.guide-toc a:hover{text-decoration:underline;background:#f7f3ff}
.guide-reference{margin-top:12px}.guide-reference h4{margin:14px 0 6px;color:var(--purple)}.guide-reference dl{margin:0}.guide-reference dt{font-weight:900;margin-top:7px}.guide-reference dd{margin:1px 0 0;color:var(--muted);font-size:12px}.guide-actions{display:grid;grid-template-columns:1fr 1fr;gap:7px}.guide-actions div{border:1px dashed var(--line);padding:8px}.guide-actions b{display:block}.guide-actions span{font-size:12px;color:var(--muted)}
.guide-reference dd span{display:block;margin-top:2px}.guide-reference dd span.metric-block-start{margin-top:10px}
.guide-drawer{overflow:hidden}.guide-content{height:calc(100vh - 73px);overflow-y:auto;padding-right:4px;scroll-behavior:smooth}
.guide-trigger{position:fixed;right:0;top:42%;z-index:30;display:flex;flex-direction:row;align-items:center;justify-content:center;gap:5px;width:58px;height:126px;background:var(--purple);color:#fff;border:2px solid var(--ink);border-right:0;border-radius:0;font-weight:900;padding:7px 5px;box-shadow:-4px 4px 0 rgba(22,24,32,.22)}.guide-trigger:hover{background:#54269c}.guide-trigger-arrow{font-size:17px;line-height:1}.guide-trigger-text{display:flex;flex-direction:column;line-height:1.05;gap:2px}.guide-drawer{position:fixed;z-index:40;top:0;right:0;width:min(360px,90vw);height:100vh;overflow-y:auto;background:var(--paper);border-left:2px solid var(--ink);box-shadow:-8px 0 24px rgba(22,24,32,.2);padding:20px;transform:translateX(100%);transition:transform .18s ease}.guide-drawer.open{transform:translateX(0)}.guide-resize{position:absolute;top:0;bottom:0;left:-7px;width:14px;cursor:ew-resize;touch-action:none}.guide-resize::after{content:'';position:absolute;top:42%;bottom:42%;left:5px;width:4px;background:var(--purple)}.guide-resize.dragging::after,.guide-resize:hover::after{top:35%;bottom:35%;background:var(--ink)}.guide-drawer-head{display:flex;align-items:center;justify-content:space-between;border-bottom:1px dashed var(--line);padding-bottom:12px;margin-bottom:16px}.guide-drawer-head b{font-size:20px}.guide-drawer-head button{padding:5px 10px}.guide-content h2{font-size:21px;margin:0 0 10px;color:var(--purple)}.guide-content h3{display:flex;justify-content:space-between;align-items:center;font-size:16px;margin:20px 0 8px;cursor:pointer;user-select:none}.guide-content h3::before{content:'# ';color:var(--purple)}.guide-content h3::after{content:'−';margin-left:auto;color:var(--purple);font-size:19px}.guide-content section.collapsed h3::after{content:'+'}.guide-content section.collapsed>:not(h3){display:none}.guide-content p{margin:0 0 12px}.guide-intro{border-left:4px solid var(--purple);background:#f7f3ff;padding:14px}.guide-overall{border:1px solid var(--ink);padding:11px;background:#fff;font-weight:800;line-height:1.7}.guide-overall span{color:var(--purple)}.guide-flow{margin:0;padding:0;list-style:none;counter-reset:flow}.guide-flow li{counter-increment:flow;display:grid;grid-template-columns:25px 1fr;gap:8px;padding:7px 0;border-bottom:1px dashed var(--line)}.guide-flow li::before{content:counter(flow);display:grid;place-items:center;width:23px;height:23px;background:var(--purple);color:#fff;font-weight:900}.guide-flow b{display:block}.guide-flow span{color:var(--muted);font-size:12px}
</style></head><body><div class="shell">
<header class="brand"><div class="brand-copy"><b class="tradelens-name"><img class="tradelens-icon" src="/icon.png" alt=""><span>TradeLens</span></b><div>Micro-backtest monitor for execution strategies</div><button id="language-switch" class="language-switch" data-language="en" role="switch" aria-checked="false" aria-label="Switch language" onclick="toggleLanguage()"><span class="language-switch-label language-en">EN</span><span class="language-track" aria-hidden="true"><span class="language-knob"></span></span><span class="language-switch-label language-vi">VN</span></button><div class="muted" id="env"></div></div><div class="vps">VPS Securities JSC</div></header>
<main id="app"></main></div><button class="guide-trigger" onclick="toggleGuide(true)" aria-label="Open Guide"><span class="guide-trigger-arrow" aria-hidden="true">◀</span><span class="guide-trigger-text"><span>G</span><span>U</span><span>I</span><span>D</span><span>E</span></span></button><aside class="guide-drawer" id="guide" aria-hidden="true"><div class="guide-resize" title="Drag to resize" onpointerdown="startGuideResize(event)" onpointermove="moveGuideResize(event)" onpointerup="stopGuideResize(event)" onpointercancel="stopGuideResize(event)"></div><div class="guide-drawer-head"><b>Guide</b><button onclick="toggleGuide(false)" aria-label="Close guide">×</button></div><div class="guide-content"><section class="guide-intro"><h2>Welcome to TradeLens</h2><p>TradeLens is a micro-backtest monitor for execution strategies.</p><p>Use it to configure an execution scenario, test parameter combinations across historical market intervals, compare performance, and replay individual sessions in detail.</p><p class="muted">The goal is to help you understand how an execution strategy behaves—not just its final result.</p></section><section><h3>Overall flow</h3><div class="guide-overall">Saved Scenarios <span>→</span> Create or reuse <span>→</span> Run <span>→</span> Compare <span>→</span> Replay <span>→</span> Refine and rerun</div><p class="muted">Open a saved scenario to view its last successful result, rerun it unchanged, or edit a copy. New results replace the previous result only after a successful run.</p></section><section><h3>Create a new scenario</h3><ol class="guide-flow"><li><div><b>Set the order</b><span>Choose symbol, BUY or SELL, and target quantity.</span></div></li><li><div><b>Build the strategy grid</b><span>Enter parameter lists; every combination becomes a strategy configuration.</span></div></li><li><div><b>Confirm history</b><span>Review the available market intervals selected for the test.</span></div></li><li><div><b>Run the backtest</b><span>Choose Debug or Release and follow live progress and logs. The last good result remains safe if a run fails.</span></div></li><li><div><b>Compare performance</b><span>Rank configurations and inspect costs, completion, fills, fees, and market divergence through time.</span></div></li><li><div><b>Replay and save</b><span>Open any interval for fills, curves, and order-book playback; then keep the scenario for reuse.</span></div></li></ol></section></div></aside>
<script>
const translations=new Map([
['SAVED SCENARIOS','KỊCH BẢN ĐÃ LƯU'],
['+ Create New Scenario','+ Tạo kịch bản mới'],
['ORDER SETUP','THIẾT LẬP LỆNH'],
['Set the order you want to execute.','Thiết lập lệnh bạn muốn thực thi.'],
['Symbol','Mã chứng khoán'],
['Side','Chiều giao dịch'],
['Quantity (shares)','Khối lượng (cổ phiếu)'],
['ORDER SUMMARY','TÓM TẮT LỆNH'],
['← Saved Scenarios','← Kịch bản đã lưu'],
['Next: Strategies →','Tiếp theo: Chiến lược →'],
['1. Order','1. Lệnh'],
['2. Strategies','2. Chiến lược'],
['3. History','3. Lịch sử'],
['4. Running','4. Đang chạy'],
['5. Performance','5. Hiệu quả'],
['6. Save','6. Lưu'],
['STRATEGY SELECTION','LỰA CHỌN CHIẾN LƯỢC'],
['Prototype strategy: TWAP. Values form a Cartesian parameter grid.','Chiến lược thử nghiệm: TWAP. Các giá trị sẽ được kết hợp thành toàn bộ tổ hợp tham số.'],
['Time-based execution','Thực thi theo thời gian'],
['Start Time (ICT)','Thời gian bắt đầu (ICT)'],
['Time Taken (seconds)','Thời gian thực thi (giây)'],
['Trade Frequency (seconds)','Tần suất giao dịch (giây)'],
['Example: 10:00:00, 10:30:00','Ví dụ: 10:00:00, 10:30:00'],
['Example: 1800, 3600','Ví dụ: 1800, 3600'],
['Example: 30, 60','Ví dụ: 30, 60'],
['Starts at the selected ICT time and submits slices until the execution window ends or the target is completed.','Bắt đầu tại thời gian ICT đã chọn và gửi từng phần lệnh cho đến khi hết cửa sổ thực thi hoặc hoàn thành mục tiêu.'],
['Time Taken','Thời gian thực thi'],
[': total execution-window duration.',': tổng thời lượng của cửa sổ thực thi.'],
['Trade Frequency','Tần suất giao dịch'],
['Slice Quantity','Khối lượng mỗi lệnh'],
[': calculated from target volume and submission opportunities, rounded up to the nearest 100 shares.',': được tính từ khối lượng mục tiêu và số lần đặt lệnh, sau đó làm tròn lên hàng trăm cổ phiếu gần nhất.'],
['The last order is capped at the remaining target quantity.','Lệnh cuối được giới hạn bằng khối lượng mục tiêu còn lại.'],
['Order Interval (seconds)','Khoảng thời gian đặt lệnh (giây)'],
['Slice Quantity (shares)','Khối lượng mỗi lần đặt lệnh (cổ phiếu)'],
['Example: 0.1, 2, 30, 40','Ví dụ: 0.1, 2, 30, 40'],
['Example: 100, 500, 1000','Ví dụ: 100, 500, 1000'],
['Estimated configurations:','Số cấu hình dự kiến:'],
['TWAP — Brief','TWAP — Mô tả ngắn'],
['Submits slices at regular time intervals until the target position is completed or the interval ends.','Gửi từng phần lệnh theo khoảng thời gian cố định cho đến khi hoàn thành khối lượng mục tiêu hoặc phiên kết thúc.'],
[': time between submission attempts.',': thời gian giữa hai lần gửi lệnh.'],
[': maximum shares submitted per attempt.',': số cổ phiếu tối đa được gửi trong mỗi lần đặt lệnh.'],
['Trading logic and backtest configuration are maintained by the developer.','Logic giao dịch và cấu hình backtest được quản lý bởi nhà phát triển.'],
['← Back','← Quay lại'],
['Next: History →','Tiếp theo: Dữ liệu lịch sử →'],
['HISTORICAL SAMPLE','MẪU DỮ LIỆU LỊCH SỬ'],
['Select historical intervals to run backtests on.','Chọn các khoảng dữ liệu lịch sử để chạy backtest.'],
['HISTORICAL SELECTION','LỰA CHỌN DỮ LIỆU LỊCH SỬ'],
['● All Available Intervals','● Tất cả khoảng dữ liệu hiện có'],
['valid intervals','khoảng dữ liệu hợp lệ'],
['configurations','cấu hình'],
['total backtests','lượt backtest tổng cộng'],
['Quantity','Khối lượng'],
['shares','cổ phiếu'],
['SELECTED INTERVALS PREVIEW','XEM TRƯỚC CÁC KHOẢNG ĐÃ CHỌN'],
['Interval','Khoảng dữ liệu'],
['Data File','Tệp dữ liệu'],
['Status','Trạng thái'],
['READY','SẴN SÀNG'],
['Resolving intervals…','Đang tìm các khoảng dữ liệu…'],
['Choose Run Mode →','Chọn chế độ chạy →'],
['CHOOSE RUN MODE','CHỌN CHẾ ĐỘ CHẠY'],
['This changes only how Cargo compiles and launches the backtest.','Lựa chọn này chỉ thay đổi cách Cargo biên dịch và khởi chạy backtest.'],
['Faster compilation, slower simulations. Best for small parameter searches and development.','Biên dịch nhanh hơn, mô phỏng chậm hơn. Phù hợp khi thử ít tổ hợp tham số hoặc đang phát triển.'],
['Slower initial compilation, faster simulations. Best for large sweeps and final runs.','Biên dịch ban đầu chậm hơn, mô phỏng nhanh hơn. Phù hợp khi chạy nhiều tổ hợp hoặc chạy kết quả cuối cùng.'],
['RUNNING BACKTEST','ĐANG CHẠY BACKTEST'],
['Cargo mode:','Chế độ Cargo:'],
['Loading…','Đang tải…'],
['Status:','Trạng thái:'],
['Started:','Bắt đầu:'],
['COMPLETED','HOÀN THÀNH'],
['RUNNING','ĐANG CHẠY'],
['PENDING','ĐANG CHỜ'],
['FAILED','THẤT BẠI'],
['CANCELLED','ĐÃ HỦY'],
['TOTAL','TỔNG'],
['Elapsed:','Đã chạy:'],
['Rate:','Tốc độ:'],
['Run finished.','Đã chạy xong.'],
['execlab is processing the selected parameter grid and intervals.','execlab đang xử lý các tổ hợp tham số và khoảng dữ liệu đã chọn.'],
['The previous successful result remains available until this attempt completes successfully.','Kết quả thành công trước đó vẫn được giữ cho đến khi lần chạy này hoàn tất thành công.'],
['Cancel Run','Hủy lần chạy'],
['View Last Successful Result','Xem kết quả thành công gần nhất'],
['View Performance','Xem hiệu quả'],
['Back to Scenarios','Quay lại danh sách kịch bản'],
['Modify Setup','Chỉnh sửa thiết lập'],
['Run Again','Chạy lại'],
['Save Scenario Details','Lưu thông tin kịch bản'],
['Saved Scenarios','Kịch bản đã lưu'],
['Guide','Hướng dẫn'],
['Welcome to TradeLens','Chào mừng đến với TradeLens'],
['Contents','Nội dung'],
['Overall flow','Quy trình tổng quan'],
['Create a new scenario','Tạo kịch bản mới'],
['Performance Summary','Tổng quan hiệu quả'],
['1. Overall flow','1. Quy trình tổng quan'],
['2. Create a new scenario','2. Tạo kịch bản mới'],
['3. Performance Summary','3. Tổng quan hiệu quả'],
['Create or reuse','Tạo mới hoặc tái sử dụng'],
['Run','Chạy'],
['Compare','So sánh'],
['Refine and rerun','Điều chỉnh và chạy lại'],
['Set the order','Thiết lập lệnh'],
['Build the strategy grid','Tạo lưới chiến lược'],
['Confirm history','Xác nhận dữ liệu lịch sử'],
['Run the backtest','Chạy backtest'],
['Compare performance','So sánh hiệu quả'],
['Replay and save','Replay và lưu'],
['Left — Strategy Comparison','Bên trái — Strategy Comparison'],
['Performance through time','Hiệu quả theo thời gian'],
['Right — Interval Explorer','Bên phải — Interval Explorer'],
['Screen actions','Thao tác trên màn hình'],
['Metric reference','Giải thích chỉ số'],
['Edit this scenario, then run the revised setup.','Chỉnh sửa kịch bản này, sau đó chạy thiết lập đã cập nhật.'],
['Rerun the current setup in Debug or Release mode.','Chạy lại thiết lập hiện tại ở chế độ Debug hoặc Release.'],
['Update its name, description, and tags.','Cập nhật tên, mô tả và các thẻ của kịch bản.'],
['Return to the scenario library.','Quay lại thư viện kịch bản.'],
['(Filled cost + residual cost + fees) ÷ initial notional × 100.','(Filled Cost + Residual Cost + fees) ÷ giá trị lệnh ban đầu × 100.'],
['Lower is better; negative means execution beat arrival.','Càng thấp càng tốt; giá trị âm nghĩa là thực thi tốt hơn giá arrival.'],
['IS% summarizes total execution performance versus the arrival mid-price, normalized by the initial order value.','IS% tổng hợp hiệu quả thực thi so với giá mid tại thời điểm bắt đầu và được chuẩn hóa theo giá trị lệnh ban đầu.'],
['Filled Cost = side sign × (trading volume × arrival mid-price − trading value).','Filled Cost = dấu chiều giao dịch × (khối lượng giao dịch × arrival mid-price − giá trị giao dịch).'],
['Side sign is +1 for SELL (positive initial position) and −1 for BUY (negative initial position).','Dấu chiều giao dịch là +1 với SELL (vị thế ban đầu dương) và −1 với BUY (vị thế ban đầu âm).'],
['Filled Cost is the realized execution cost versus arrival: positive is worse and negative is better.','Filled Cost là chi phí thực thi đã khớp so với giá arrival: giá trị dương là kém hơn và giá trị âm là tốt hơn.'],
['Residual Cost = final inventory × (arrival mid-price − final mid-price).','Residual Cost = vị thế cuối × (arrival mid-price − final mid-price).'],
['Residual Cost is the mark-to-market opportunity cost of the unexecuted final inventory.','Residual Cost là chi phí cơ hội mark-to-market của phần vị thế cuối chưa được thực thi.'],
['15th, median, and 90th percentile of interval IS%.','Phân vị 15, trung vị và phân vị 90 của IS% theo từng khoảng dữ liệu.'],
['Mean IS% is the interval average.','Mean IS% là giá trị trung bình của các khoảng dữ liệu.'],
['100 − final inventory ÷ initial position × 100.','100 − vị thế cuối ÷ vị thế ban đầu × 100.'],
['Higher is better.','Càng cao càng tốt.'],
['Completion% shows how much of the target position was executed by the end of the interval; 100% means the target was fully completed.','Completion% cho biết phần khối lượng mục tiêu đã được thực thi khi khoảng dữ liệu kết thúc; 100% nghĩa là đã hoàn thành toàn bộ mục tiêu.'],
['Fees accumulated in one interval.','Phí tích lũy trong một khoảng dữ liệu.'],
['Strategy Avg Fees$ is the arithmetic average across intervals.','Avg Fees$ của chiến lược là trung bình cộng trên các khoảng dữ liệu.'],
['Executed trades and submitted orders.','Số giao dịch đã khớp và số lệnh đã gửi.'],
['Strategy Avg #Trades is the interval average.','Avg #Trades của chiến lược là giá trị trung bình trên các khoảng dữ liệu.'],
['#Trades ÷ #Orders × 100; this is order/trade based, not quantity completion.','#Trades ÷ #Orders × 100; chỉ số này dựa trên số lệnh và giao dịch, không phải mức hoàn thành theo khối lượng.'],
['Strategy Avg %Filled is the interval average.','Avg %Filled của chiến lược là giá trị trung bình trên các khoảng dữ liệu.'],
['Average or maximum effective-spread ÷ normal-spread score sampled during an interval.','Điểm trung bình hoặc lớn nhất của effective spread ÷ normal spread được ghi nhận trong một khoảng dữ liệu.'],
['Strategy values average the interval scores.','Giá trị của chiến lược là trung bình các điểm theo khoảng dữ liệu.'],
['It indicates the strategy’s simulated impact on the order book: 1 means no spread impact, while a value above 1 means the effective spread is wider than the normal spread.','Chỉ số này thể hiện tác động mô phỏng của chiến lược lên sổ lệnh: 1 nghĩa là không tác động đến spread; lớn hơn 1 nghĩa là effective spread rộng hơn normal spread.'],
['OK when the interval has no recorded error.','OK khi khoảng dữ liệu không ghi nhận lỗi.'],
['Strategy Status is the percentage of OK intervals.','Status của chiến lược là tỷ lệ phần trăm các khoảng dữ liệu có trạng thái OK.'],
['Reusable execution setups. Choose a widget to run, edit, duplicate, or inspect its latest result.','Các thiết lập thực thi có thể tái sử dụng. Chọn một thẻ để chạy, chỉnh sửa, sao chép hoặc xem kết quả gần nhất.'],
['Micro-backtest monitor for execution strategies','Công cụ theo dõi micro-backtest cho chiến lược thực thi'],
['TradeLens is a micro-backtest monitor for execution strategies.','TradeLens là công cụ theo dõi micro-backtest dành cho các chiến lược thực thi.'],
['Use it to configure an execution scenario, test parameter combinations across historical market intervals, compare performance, and replay individual sessions in detail.','Dùng TradeLens để thiết lập kịch bản thực thi, kiểm thử các tổ hợp tham số trên dữ liệu lịch sử, so sánh hiệu quả và xem lại chi tiết từng phiên.'],
['The goal is to help you understand how an execution strategy behaves—not just its final result.','Mục tiêu là giúp bạn hiểu cách chiến lược thực thi vận hành, không chỉ nhìn vào kết quả cuối cùng.'],
['Open a saved scenario to view its last successful result, rerun it unchanged, or edit a copy. New results replace the previous result only after a successful run.','Mở kịch bản đã lưu để xem kết quả thành công gần nhất, chạy lại nguyên trạng hoặc chỉnh sửa một bản sao. Kết quả mới chỉ thay thế kết quả cũ sau khi chạy thành công.'],
['Choose symbol, BUY or SELL, and target quantity.','Chọn mã, chiều BUY hoặc SELL và khối lượng mục tiêu.'],
['Enter parameter lists; every combination becomes a strategy configuration.','Nhập danh sách tham số; mỗi tổ hợp sẽ trở thành một cấu hình chiến lược.'],
['Review the available market intervals selected for the test.','Kiểm tra các khoảng dữ liệu thị trường được chọn để chạy thử.'],
['Choose Debug or Release and follow live progress and logs. The last good result remains safe if a run fails.','Chọn Debug hoặc Release và theo dõi tiến độ cùng log trực tiếp. Kết quả tốt gần nhất vẫn được giữ nếu lần chạy thất bại.'],
['Rank configurations and inspect costs, completion, fills, fees, and market divergence through time.','Xếp hạng cấu hình và kiểm tra chi phí, mức hoàn thành, giao dịch khớp, phí và độ phân kỳ thị trường theo thời gian.'],
['Open any interval for fills, curves, and order-book playback; then keep the scenario for reuse.','Mở từng khoảng để xem fills, curves và phát lại sổ lệnh; sau đó lưu kịch bản để tái sử dụng.'],
['The screen is a two-column workspace. Drag the center divider to give either side more room.','Màn hình gồm hai cột. Kéo đường phân cách ở giữa để điều chỉnh không gian mỗi bên.'],
['Each row is one parameter configuration. Click rows to show or hide them on every chart; the last clicked row becomes the active strategy. Tables support sorting and paging.','Mỗi dòng là một cấu hình tham số. Nhấn vào dòng để hiện hoặc ẩn trên mọi biểu đồ; dòng được nhấn gần nhất là chiến lược đang hoạt động. Bảng hỗ trợ sắp xếp và phân trang.'],
['Charts show IS%, Completion%, %Filled, Fees, and Mean Divergence for each historical interval in ICT. Hover to compare values, drag to pan, and zoom; the time axis is synchronized. Click a legend color to change it.','Biểu đồ hiển thị IS%, Completion%, %Filled, Fees và Mean Divergence theo từng khoảng lịch sử ở múi giờ ICT. Di chuột để so sánh, kéo để dịch chuyển và zoom; trục thời gian được đồng bộ. Nhấn màu chú thích để đổi màu.'],
['Inspect every session for the active strategy. Sort or page through interval statistics, then click Replay to open fills, account curves, price charts, and order-book playback below the table.','Kiểm tra từng phiên của chiến lược đang chọn. Sắp xếp hoặc phân trang thống kê, rồi nhấn Replay để mở fills, account curves, biểu đồ giá và phát lại sổ lệnh bên dưới bảng.'],
['Use Strategy Comparison to find promising configurations, then Interval Explorer to understand where and why they performed well or poorly.','Dùng Strategy Comparison để tìm cấu hình tiềm năng, sau đó dùng Interval Explorer để hiểu chiến lược hoạt động tốt hoặc kém ở đâu và vì sao.'],
['Click rows to select/unselect chart lines. Last clicked strategy drives Interval Explorer.','Nhấn vào dòng để chọn hoặc bỏ chọn đường biểu đồ. Chiến lược được nhấn gần nhất sẽ điều khiển Interval Explorer.'],
['Top 3 bid and ask levels. Frames are discrete replay samples; no interpolation.','Ba mức bid và ask tốt nhất. Mỗi khung là một mẫu replay rời rạc, không nội suy.'],
['Blue = bid liquidity · Orange = ask liquidity · bar width is relative quantity within the selected frame.','Xanh = thanh khoản bid · Cam = thanh khoản ask · độ rộng thanh biểu thị khối lượng tương đối trong khung đang chọn.'],
['Executed fill price through time (ICT).','Giá khớp lệnh theo thời gian (ICT).'],
['Effective best bid and ask after simulated impact.','Best bid và ask hiệu dụng sau tác động mô phỏng.'],
['Inventory through the session.','Vị thế trong suốt phiên.'],
['Cash balance through the session.','Số dư tiền trong suốt phiên.'],
['Marked equity through the session.','Giá trị tài sản theo mark-to-market trong suốt phiên.'],
[
'IS% measures total execution performance versus the arrival mid-price, normalized by the initial order value. Lower is better; a negative value means the execution beat the arrival price.',
'IS% đo lường tổng hiệu quả thực thi so với giá mid tại thời điểm bắt đầu, được chuẩn hóa theo giá trị lệnh ban đầu. Càng thấp càng tốt; giá trị âm nghĩa là thực thi tốt hơn giá lúc bắt đầu.'
],
[
'Completion% measures how much of the target position was executed by the end of the interval. Higher is better; 100% means the target was fully completed.',
'Completion% đo lường phần khối lượng mục tiêu đã được thực thi khi khoảng dữ liệu kết thúc. Càng cao càng tốt; 100% nghĩa là đã hoàn thành toàn bộ mục tiêu.'
],
[
'%Filled is the number of trades divided by the number of submitted orders. Higher means more submitted orders produced trades; it is not quantity completion.',
'%Filled là số giao dịch khớp chia cho số lệnh đã gửi. Giá trị cao hơn nghĩa là nhiều lệnh gửi đi tạo ra giao dịch hơn; đây không phải tỷ lệ hoàn thành theo khối lượng.'
],
[
'Fees show the total transaction fees recorded in each interval. Lower is better when comparing strategies with similar executed volume.',
'Fees là tổng phí giao dịch được ghi nhận trong từng khoảng dữ liệu. Càng thấp càng tốt khi so sánh các chiến lược có khối lượng thực thi tương đương.'
],
[
'Mean Divergence Score measures simulated order-book impact as effective spread divided by normal spread. A score of 1 means no spread impact; lower is better.',
'Mean Divergence Score đo lường tác động mô phỏng lên sổ lệnh bằng effective spread chia cho normal spread. Điểm 1 nghĩa là không làm thay đổi spread; càng thấp càng tốt.'
]
]);
const translationsVi=new Map([...translations].map(([en,vi])=>[vi,en]));
let uiLanguage=localStorage.getItem('tradeLensLanguage')==='vi'?'vi':'en';
function translateTextNode(node){if(node.parentElement?.closest('.perf table'))return;const raw=node.nodeValue,trimmed=raw.trim();if(!trimmed)return;const translated=(uiLanguage==='vi'?translations:translationsVi).get(trimmed);if(translated!==undefined)node.nodeValue=raw.replace(trimmed,translated)}
function applyLanguage(root=document){const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);while(walker.nextNode())translateTextNode(walker.currentNode);document.documentElement.lang=uiLanguage;const languageSwitch=document.querySelector('#language-switch');if(languageSwitch){languageSwitch.dataset.language=uiLanguage;languageSwitch.setAttribute('aria-checked',String(uiLanguage==='vi'))}}
function setLanguage(language){if(language===uiLanguage)return;uiLanguage=language;localStorage.setItem('tradeLensLanguage',language);applyLanguage();if(document.querySelector('#order-summary'))updateSummary()}
function toggleLanguage(){setLanguage(uiLanguage==='en'?'vi':'en')}
new MutationObserver(records=>records.forEach(record=>record.addedNodes.forEach(node=>{if(node.nodeType===Node.TEXT_NODE)translateTextNode(node);else if(node.nodeType===Node.ELEMENT_NODE)applyLanguage(node)}))).observe(document.body,{childList:true,subtree:true});
const app=document.querySelector('#app');let meta={},draft=null,current=null,currentScenario=null,perf=null,activeStrategy=0,selectedStrategies=new Set([0]),strategyPage=1,strategyPageSize=5,intervalPage=1,intervalPageSize=5,strategySort={key:'mean_is_pct',dir:'asc'},intervalSort={key:'session_start_ts',dir:'asc'},replayData={fills:[],curve:[]},replayTables={fills:{page:1,size:5,sort:'ts',dir:'asc'},curve:{page:1,size:5,sort:'ts',dir:'asc'}};
let orderbookFrames=[],orderbookIndex=0,orderbookTimer=null;
let guideResize=null;
function toggleGuide(open){const drawer=document.querySelector('#guide'),saved=Number(localStorage.getItem('tradeLensGuideWidth'));if(open&&Number.isFinite(saved))drawer.style.width=Math.min(innerWidth-70,Math.max(280,saved))+'px';drawer.classList.toggle('open',open);drawer.setAttribute('aria-hidden',String(!open))}
document.querySelector('.guide-content').insertAdjacentHTML('beforeend',`<section><h3>Performance Summary</h3><p>The screen is a two-column workspace. Drag the center divider to give either side more room.</p><ol class="guide-flow"><li><div><b>Left — Strategy Comparison</b><span>Each row is one parameter configuration. Click rows to show or hide them on every chart; the last clicked row becomes the active strategy. Tables support sorting and paging.</span></div></li><li><div><b>Performance through time</b><span>Charts show IS%, Completion%, %Filled, Fees, and Mean Divergence for each historical interval in ICT. Hover to compare values, drag to pan, and zoom; the time axis is synchronized. Click a legend color to change it.</span></div></li><li><div><b>Right — Interval Explorer</b><span>Inspect every session for the active strategy. Sort or page through interval statistics, then click Replay to open fills, account curves, price charts, and order-book playback below the table.</span></div></li></ol><p class="muted">Use Strategy Comparison to find promising configurations, then Interval Explorer to understand where and why they performed well or poorly.</p></section>`);
document.querySelector('.guide-content section:last-child').insertAdjacentHTML('beforeend',`<div class="guide-reference"><h4>Screen actions</h4><div class="guide-actions"><div><b>Modify Setup</b><span>Edit this scenario, then run the revised setup.</span></div><div><b>Run Again</b><span>Rerun the current setup in Debug or Release mode.</span></div><div><b>Save Scenario Details</b><span>Update its name, description, and tags.</span></div><div><b>Saved Scenarios</b><span>Return to the scenario library.</span></div></div><h4>Metric reference</h4><dl><dt>IS%</dt><dd>(Filled cost + residual cost + fees) ÷ initial notional × 100. Lower is better; negative means execution beat arrival.</dd><dt>P15 / P50 / P90 IS%</dt><dd>15th, median, and 90th percentile of interval IS%. Mean IS% is the interval average.</dd><dt>Completion%</dt><dd>100 − final inventory ÷ initial position × 100. Higher is better.</dd><dt>Fees$</dt><dd>Fees accumulated in one interval. Strategy Avg Fees$ is the arithmetic average across intervals.</dd><dt>#Trades / #Orders</dt><dd>Executed trades and submitted orders. Strategy Avg #Trades is the interval average.</dd><dt>%Filled</dt><dd>#Trades ÷ #Orders × 100; this is order/trade based, not quantity completion. Strategy Avg %Filled is the interval average.</dd><dt>Mean / Max Divergence</dt><dd>Average or maximum effective-spread ÷ normal-spread score sampled during an interval. Strategy values average the interval scores.</dd><dt>Status</dt><dd>OK when the interval has no recorded error. Strategy Status is the percentage of OK intervals.</dd></dl></div>`);
const metricTerms=[...document.querySelectorAll('.guide-reference dt')];metricTerms[0].textContent='IS% (Implementation Shortfall %)';metricTerms[0].nextElementSibling.append(' IS% summarizes total execution performance versus the arrival mid-price, normalized by the initial order value. Filled Cost = side sign × (trading volume × arrival mid-price − trading value). Side sign is +1 for SELL (positive initial position) and −1 for BUY (negative initial position). Filled Cost is the realized execution cost versus arrival: positive is worse and negative is better. Residual Cost = final inventory × (arrival mid-price − final mid-price). Residual Cost is the mark-to-market opportunity cost of the unexecuted final inventory.');metricTerms[2].nextElementSibling.append(' Completion% shows how much of the target position was executed by the end of the interval; 100% means the target was fully completed.');metricTerms[6].nextElementSibling.append(' It indicates the strategy’s simulated impact on the order book: 1 means no spread impact, while a value above 1 means the effective spread is wider than the normal spread.');metricTerms.forEach(term=>{const detail=term.nextElementSibling,lines=detail.textContent.split('. ').filter(Boolean);detail.replaceChildren(...lines.map((line,index)=>{const span=document.createElement('span');span.textContent=line+(index<lines.length-1&&!line.endsWith('.')?'.':'');if(line.startsWith('Filled Cost =')||line.startsWith('Residual Cost ='))span.className='metric-block-start';return span}))});
const guideSections=[...document.querySelectorAll('.guide-content section:not(.guide-intro)')];
guideSections.forEach((section,index)=>{const heading=section.querySelector('h3');section.id=`guide-section-${index+1}`;heading.tabIndex=0;heading.setAttribute('role','button');heading.setAttribute('aria-expanded','true');const toggle=()=>{const collapsed=section.classList.toggle('collapsed');heading.setAttribute('aria-expanded',String(!collapsed))};heading.onclick=toggle;heading.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle()}}});
const guideToc=document.createElement('nav');guideToc.className='guide-toc';guideToc.setAttribute('aria-label','Guide sections');guideToc.innerHTML='<b>Contents</b>'+guideSections.map((section,index)=>`<a href="#${section.id}" data-guide-index="${index}">${index+1}. ${section.querySelector('h3').textContent}</a>`).join('');document.querySelector('.guide-intro').after(guideToc);guideToc.querySelectorAll('a').forEach(link=>link.onclick=e=>{e.preventDefault();const section=guideSections[Number(link.dataset.guideIndex)],heading=section.querySelector('h3');section.classList.remove('collapsed');heading.setAttribute('aria-expanded','true');section.scrollIntoView({behavior:'smooth',block:'start'})});
function startGuideResize(e){const drawer=document.querySelector('#guide');guideResize={x:e.clientX,width:drawer.getBoundingClientRect().width};e.currentTarget.setPointerCapture(e.pointerId);e.currentTarget.classList.add('dragging');document.body.style.userSelect='none'}
function moveGuideResize(e){if(!guideResize)return;const width=Math.min(innerWidth-70,Math.max(280,guideResize.width+guideResize.x-e.clientX));document.querySelector('#guide').style.width=width+'px'}
function stopGuideResize(e){if(!guideResize)return;const handle=e.currentTarget,width=document.querySelector('#guide').getBoundingClientRect().width;localStorage.setItem('tradeLensGuideWidth',String(Math.round(width)));guideResize=null;handle.classList.remove('dragging');document.body.style.userSelect='';if(handle.hasPointerCapture(e.pointerId))handle.releasePointerCapture(e.pointerId)}
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(path,opt={}){const r=await fetch(path,{headers:{'Content-Type':'application/json'},...opt});const v=await r.json().catch(()=>({error:r.statusText}));if(!r.ok)throw Error(v.error||r.statusText);return v}
function fail(e){app.insertAdjacentHTML('afterbegin',`<div class="error">${esc(e.message||e)}</div>`)}
function steps(n){return `<div class="steps">${['Order','Strategies','History','Running','Performance','Save'].map((s,i)=>`<div class="step ${i+1===n?'on':''}">${i+1}. ${s}</div>`).join('')}</div>`}
function defaultDraft(){return {name:'',description:'',tags:[],order:{symbol:meta.symbols?.[0]||'',side:'sell',quantity:1000},strategies:[{name:'twap_sell',inputs:{start_times:['10:00:00'],time_taken_seconds:[1800,3600],trade_frequency_seconds:[30,60]}}]}}
function parseNums(s){const a=[...new Set(s.split(',').map(v=>Number(v.trim())).filter(v=>Number.isFinite(v)&&v>0))];if(!a.length)throw Error('Enter at least one positive number');return a}
function parseTimes(s){const values=[...new Set(s.split(',').map(v=>v.trim()).filter(Boolean).map(v=>{const match=v.match(/^(\d{1,2}):(\d{2})(?::(\d{2}))?$/);if(!match)throw Error('Start times must use HH:MM or HH:MM:SS');const h=Number(match[1]),m=Number(match[2]),sec=Number(match[3]||0);if(h>23||m>59||sec>59)throw Error('Start time is outside 00:00:00–23:59:59');return [h,m,sec].map(n=>String(n).padStart(2,'0')).join(':') }))];if(!values.length)throw Error('Enter at least one start time');return values}
async function home(){current=null;const rows=await api('/api/scenarios');app.innerHTML=`<div class="row spread"><div><div class="title">SAVED SCENARIOS</div><div class="sub">Reusable execution setups. Run, modify, duplicate, or inspect the latest successful result.</div></div><button class="primary" onclick="newScenario()">+ Create New Scenario</button></div><div class="panel"><div class="row"><input id="search" placeholder="Search scenarios…" oninput="filterScenarios()"><span>${rows.length} scenario(s)</span></div><div id="scenario-list">${rows.length?rows.map(scenarioCard).join(''):'<div class="empty">No saved scenarios yet.</div>'}</div></div>`;window._scenarios=rows}
function scenarioCard(s){const p=s.strategies[0].inputs,configs=p.start_times.length*p.time_taken_seconds.length*p.trade_frequency_seconds.length;return `<div class="scenario" data-search="${esc((s.name+' '+s.order.symbol+' '+s.order.side).toLowerCase())}"><div><h3>${esc(s.name)}</h3><div class="row"><span class="tag">${esc(s.order.symbol)}</span><span class="tag">${esc(s.order.side.toUpperCase())}</span><span class="tag">TWAP</span></div><div class="muted">Quantity ${s.order.quantity.toLocaleString()} · ${configs} configs · All available intervals</div><div>Latest success: ${s.latest_successful_run_id?new Date(s.updated_at*1000).toLocaleString():'—'}</div></div><div class="stack"><button class="primary" onclick="runExisting('${s.id}')">RUN →</button><button onclick="viewResult('${s.id}')">View Last Result</button><button onclick="editScenario('${s.id}')">Edit Setup</button><button onclick="duplicateScenario('${s.id}')">Duplicate</button><button onclick="renameScenario('${s.id}')">Rename</button><button class="danger" onclick="deleteScenario('${s.id}')">Delete</button></div></div>`}
home=async function(){current=null;const rows=await api('/api/scenarios');app.innerHTML=`<div class="row spread"><div><div class="title">SAVED SCENARIOS</div><div class="sub">Reusable execution setups. Choose a widget to run, edit, duplicate, or inspect its latest result.</div></div><button class="primary" onclick="newScenario()">+ Create New Scenario</button></div><div class="panel"><div class="row spread"><input id="search" placeholder="Search scenarios…" oninput="filterScenarios()"><b>${rows.length} scenario(s)</b></div><div id="scenario-list" class="scenario-grid">${rows.length?rows.map(scenarioCard).join(''):'<div class="empty">No saved scenarios yet.</div>'}</div></div>`;window._scenarios=rows};
scenarioCard=function(s){const strategy=s.strategies?.[0],inputs=strategy?.inputs||{},configs=(inputs.start_times?.length||0)*(inputs.time_taken_seconds?.length||0)*(inputs.trade_frequency_seconds?.length||0),ready=Boolean(s.latest_successful_run_id),tags=(s.tags||[]).map(tag=>`<span class="tag">${esc(tag)}</span>`).join(''),search=[s.name,s.order.symbol,s.order.side,s.description,...(s.tags||[])].join(' ').toLowerCase(),side=String(s.order.side).toUpperCase(),strategyLabel=String(strategy?.name||'TWAP').replace(/_(buy|sell)$/i,'').replaceAll('_',' ').toUpperCase();return `<article class="scenario" data-search="${esc(search)}"><div class="scenario-top"><h3>${esc(s.name)}</h3><span class="scenario-status ${ready?'ready':'new'}">${ready?'RESULT READY':'NOT RUN'}</span></div><div class="row"><span class="tag">${esc(side)}</span><span class="tag">${esc(strategyLabel)}</span>${tags}</div><div class="scenario-description">${esc(s.description||'No description')}</div><div class="scenario-facts"><div class="scenario-fact"><small>SYMBOL</small><strong class="scenario-symbol">${esc(s.order.symbol)}</strong></div><div class="scenario-fact"><small>SIDE</small><strong>${esc(side)}</strong></div><div class="scenario-fact"><small>TARGET VOLUME</small><strong>${Number(s.order.quantity).toLocaleString()}</strong></div><div class="scenario-fact"><small>CONFIGURATIONS</small><strong>${configs}</strong></div><div class="scenario-fact"><small>HISTORY</small><strong>ALL</strong></div></div><div class="scenario-latest"><span class="muted">Latest successful run</span><br><b>${ready?new Date(s.updated_at*1000).toLocaleString():'—'}</b></div><div class="scenario-actions"><button class="primary scenario-run" onclick="runExisting('${s.id}')">RUN SCENARIO →</button><button class="scenario-view" onclick="viewResult('${s.id}')">View Last Result</button><button onclick="editScenario('${s.id}')">Edit</button><button onclick="duplicateScenario('${s.id}')">Copy</button><button onclick="renameScenario('${s.id}')">Rename</button><button class="danger" onclick="deleteScenario('${s.id}')">Delete</button></div></article>`};
const renderScenarioCard=scenarioCard;
scenarioCard=function(s){const side=String(s.order.side).toLowerCase(),sideLabel=esc(side.toUpperCase());return renderScenarioCard(s).replace(`<span class="tag">${sideLabel}</span>`,`<span class="tag scenario-side side-${side}">${sideLabel}</span>`).replace(`<strong>${sideLabel}</strong>`,`<strong class="scenario-side-text side-${side}">${sideLabel}</strong>`)};
const renderScenarioCardWithSide=scenarioCard;
scenarioCard=function(s){const best=s.best_strategy,p=best?.params||{},paramText=best?strategyParamsLabel(p):'No successful result yet',pct=(v,d=3)=>v==null?'—':`${fmt(v,d)}%`,cell=(label,value,cls='')=>`<span><small>${label}</small><b class="${cls}">${value}</b></span>`,bestHtml=`<div class="scenario-best"><div class="scenario-best-title">BEST STRATEGY</div><div class="scenario-best-params">${paramText}</div>${best?`<div class="scenario-best-metrics">${cell('MEAN IS',pct(best.mean_is_pct),costClass(best.mean_is_pct))}${cell('P15 IS',pct(best.p15_is_pct),costClass(best.p15_is_pct))}${cell('P50 IS',pct(best.p50_is_pct),costClass(best.p50_is_pct))}${cell('P90 IS',pct(best.p90_is_pct),costClass(best.p90_is_pct))}${cell('COMPLETION',pct(best.completion_pct,1))}${cell('AVG FEES$',feeFmt(best.avg_fee))}${cell('AVG #TRADES',fmt(best.avg_trades,2))}${cell('AVG MEAN DIV.',fmt(best.avg_mean_divergence))}${cell('AVG MAX DIV.',fmt(best.avg_max_divergence))}${cell('STATUS',pct(best.status_pct,1),best.status_pct>=95?'good':'bad')}</div>`:'<div class="scenario-best-empty">Run this scenario to rank its configurations.</div>'}</div>`;return renderScenarioCardWithSide(s).replace('<div class="scenario-latest">',bestHtml+'<div class="scenario-latest">')};
function filterScenarios(){const q=document.querySelector('#search').value.toLowerCase();document.querySelectorAll('.scenario').forEach(e=>e.classList.toggle('hidden',!e.dataset.search.includes(q)))}
function newScenario(){draft=defaultDraft();current=null;orderScreen()}
async function editScenario(id){draft=await api('/api/scenarios/'+id);current=id;orderScreen()}
async function duplicateScenario(id){await api(`/api/scenarios/${id}/duplicate`,{method:'POST'});home()}
async function renameScenario(id){const s=await api('/api/scenarios/'+id),name=prompt('Scenario name',s.name);if(!name)return;s.name=name;await api('/api/scenarios/'+id,{method:'PUT',body:JSON.stringify(s)});home()}
async function deleteScenario(id){if(confirm('Delete this scenario? Run files are retained for now.')){await api('/api/scenarios/'+id,{method:'DELETE'});home()}}
function orderScreen(){app.innerHTML=steps(1)+`<div class="title">ORDER SETUP</div><div class="sub">Set the order you want to execute.</div><div class="panel grid2"><div class="stack"><label>Symbol<select id="symbol">${meta.symbols.map(s=>`<option ${s===draft.order.symbol?'selected':''}>${esc(s)}</option>`).join('')}</select></label><label>Side<select id="side"><option value="buy" ${draft.order.side==='buy'?'selected':''}>BUY</option><option value="sell" ${draft.order.side==='sell'?'selected':''}>SELL</option></select></label><label>Quantity (shares)<input id="qty" type="number" min="0" step="any" value="${draft.order.quantity}"></label></div><div class="panel dashed"><b>ORDER SUMMARY</b><hr><div id="order-summary"></div></div></div><div class="row spread"><button onclick="home()">← Saved Scenarios</button><button class="primary" onclick="toStrategy()">Next: Strategies →</button></div>`;['symbol','side','qty'].forEach(id=>document.querySelector('#'+id).oninput=updateSummary);updateSummary()}
function updateSummary(){const symbol=esc(document.querySelector('#symbol').value),side=esc(document.querySelector('#side').value.toUpperCase()),quantity=Number(document.querySelector('#qty').value||0).toLocaleString();document.querySelector('#order-summary').textContent=uiLanguage==='vi'?`${side} ${quantity} cổ phiếu ${symbol}`:`${side} ${quantity} shares of ${symbol}`}
function captureOrder(){const q=Number(document.querySelector('#qty').value);if(!(q>0))throw Error('Quantity must be positive');draft.order={symbol:document.querySelector('#symbol').value,side:document.querySelector('#side').value,quantity:q}}
function toStrategy(){try{captureOrder();strategyScreen()}catch(e){fail(e)}}
function strategyScreen(){const p=draft.strategies[0].inputs;app.innerHTML=steps(2)+`<div class="title">STRATEGY SELECTION</div><div class="sub">Prototype strategy: TWAP. Values form a Cartesian parameter grid.</div><div class="grid2"><div class="panel stack"><div class="row spread"><b>[×] TWAP</b><span class="tag">Time-based execution</span></div><label>Start Time (ICT)<input id="start-times" value="${p.start_times.join(', ')}"><small class="muted">Example: 10:00:00, 10:30:00</small></label><label>Time Taken (seconds)<input id="time-taken" value="${p.time_taken_seconds.join(', ')}"><small class="muted">Example: 1800, 3600</small></label><label>Trade Frequency (seconds)<input id="trade-frequency" value="${p.trade_frequency_seconds.join(', ')}"><small class="muted">Example: 30, 60</small></label><div class="metric">Estimated configurations: <b id="config-count"></b></div></div><div class="panel dashed"><b>TWAP — Brief</b><p>Starts at the selected ICT time and submits slices until the execution window ends or the target is completed.</p><p><b>Time Taken</b>: total execution-window duration.</p><p><b>Trade Frequency</b>: time between submission attempts.</p><p><b>Slice Quantity</b>: calculated from target volume and submission opportunities, rounded up to the nearest 100 shares.</p><p class="muted">The last order is capped at the remaining target quantity.</p></div></div><div class="row spread"><button onclick="orderScreen()">← Back</button><button class="primary" onclick="toHistory()">Next: History →</button></div>`;['start-times','time-taken','trade-frequency'].forEach(id=>document.querySelector('#'+id).oninput=countConfigs);countConfigs()}
function countConfigs(){try{const a=parseTimes(document.querySelector('#start-times').value),b=parseNums(document.querySelector('#time-taken').value),c=parseNums(document.querySelector('#trade-frequency').value);document.querySelector('#config-count').textContent=`${a.length} × ${b.length} × ${c.length} = ${a.length*b.length*c.length}`}catch(e){document.querySelector('#config-count').textContent='Invalid input'}}
async function toHistory(){try{draft.strategies[0].inputs={start_times:parseTimes(document.querySelector('#start-times').value),time_taken_seconds:parseNums(document.querySelector('#time-taken').value),trade_frequency_seconds:parseNums(document.querySelector('#trade-frequency').value)};await historyScreen()}catch(e){fail(e)}}
async function historyScreen(){app.innerHTML=steps(3)+'<div class="empty">Resolving intervals…</div>';try{const intervals=await api('/api/intervals?symbol='+encodeURIComponent(draft.order.symbol)),p=draft.strategies[0].inputs,n=p.start_times.length*p.time_taken_seconds.length*p.trade_frequency_seconds.length;window._intervals=intervals;app.innerHTML=steps(3)+`<div class="title">HISTORICAL SAMPLE</div><div class="sub">Select historical intervals to run backtests on.</div><div class="grid2"><div><div class="panel"><b>HISTORICAL SELECTION</b><p>● All Available Intervals</p><div class="metric">${intervals.length} <span>valid intervals</span><br>${n} <span>configurations</span><br><b>${intervals.length*n} <span>total backtests</span></b></div></div><div class="panel dashed"><span>Symbol</span>: ${esc(draft.order.symbol)}<br><span>Side</span>: ${esc(draft.order.side.toUpperCase())}<br><span>Quantity</span>: ${draft.order.quantity.toLocaleString()} <span>shares</span></div></div><div class="panel scroll"><b>SELECTED INTERVALS PREVIEW</b><table><thead><tr><th>#</th><th>Interval</th><th>Data File</th><th>Status</th></tr></thead><tbody>${intervals.map((v,i)=>`<tr><td>${i+1}</td><td>${esc(v.id)}</td><td>${esc(v.file)}</td><td class="good">READY</td></tr>`).join('')}</tbody></table></div></div><div class="row spread"><button onclick="strategyScreen()">← Back</button><button class="primary" ${intervals.length?'':'disabled'} onclick="runModeScreen('draft')">Choose Run Mode →</button></div>`}catch(e){fail(e)}}
async function saveSetup(){draft.name=draft.name||`${draft.order.symbol} ${draft.order.side.toUpperCase()} TWAP`;return current?api('/api/scenarios/'+current,{method:'PUT',body:JSON.stringify(draft)}):api('/api/scenarios',{method:'POST',body:JSON.stringify(draft)})}
function runModeScreen(source){window._runSource=source;app.innerHTML=steps(4)+`<div class="title">CHOOSE RUN MODE</div><div class="sub">This changes only how Cargo compiles and launches the backtest.</div><div class="run-mode-grid"><button class="run-mode" onclick="startSelectedRun('debug')"><b>DEBUG</b><span>Faster compilation, slower simulations. Best for small parameter searches and development.</span></button><button class="run-mode" onclick="startSelectedRun('release')"><b>RELEASE</b><span>Slower initial compilation, faster simulations. Best for large sweeps and final runs.</span></button></div><div class="row" style="margin-top:18px"><button onclick="${source==='draft'?'historyScreen()':'home()'}">← Back</button></div>`}
async function startSelectedRun(mode){try{if(window._runSource==='draft'){const s=await saveSetup();current=s.id}await api(`/api/scenarios/${current}/run?mode=${mode}`,{method:'POST'});runningScreen(mode)}catch(e){fail(e)}}
async function runExisting(id){current=id;runModeScreen('existing')}
async function runningScreen(mode){app.innerHTML=steps(4)+`<div class="row spread"><div><div class="title">RUNNING BACKTEST</div><div class="sub">Cargo mode: <b>${esc(String(mode||'debug').toUpperCase())}</b></div></div></div><div id="run-body" class="panel">Loading…</div><div class="panel run-log-panel"><div class="run-log-head"><b>LIVE RUN LOG</b><span class="muted">Latest 64 KiB</span></div><pre id="run-log" class="run-log">Waiting for process output…</pre></div><div class="row spread"><button class="danger" onclick="cancelRun()">Cancel Run</button><button onclick="viewResult('${current}')">View Last Successful Result</button></div>`;pollRun()}
function duration(seconds){seconds=Math.max(0,Math.round(Number(seconds)||0));const h=Math.floor(seconds/3600),m=Math.floor(seconds%3600/60),s=seconds%60;return [h,m,s].map(v=>String(v).padStart(2,'0')).join(':')}
function updateRunLog(text){const log=document.querySelector('#run-log');if(!log)return;const follow=log.scrollHeight-log.scrollTop-log.clientHeight<30;log.textContent=text||'Waiting for process output…';if(follow)log.scrollTop=log.scrollHeight}
async function pollRun(){if(!current)return;try{const r=await api(`/api/scenarios/${current}/latest-run`),p=r.progress||{},done=['completed','failed','cancelled'].includes(r.status),percent=Math.max(0,Math.min(100,Number(p.percent)||0));document.querySelector('#run-body').innerHTML=`<div class="row spread"><b>Status: <span class="${r.status==='completed'?'good':r.status==='failed'?'bad':''}">${r.status.toUpperCase()}</span></b><span>Started: ${r.started_at?new Date(r.started_at*1000).toLocaleString():'—'}</span><b>${fmt(percent,1)}% (${p.processed||0} / ${p.total||0})</b></div><div class="progress"><div class="bar" style="width:${percent}%"></div></div><div class="run-stats"><div class="run-stat"><small>COMPLETED</small><strong class="good">${p.completed||0}</strong></div><div class="run-stat"><small>RUNNING</small><strong>${p.running||0}</strong></div><div class="run-stat"><small>PENDING</small><strong>${p.pending||0}</strong></div><div class="run-stat"><small>FAILED</small><strong class="${p.failed?'bad':''}">${p.failed||0}</strong></div><div class="run-stat"><small>TOTAL</small><strong>${p.total||0}</strong></div></div><div class="run-timing"><span>Elapsed: <b>${duration(p.elapsed_sec)}</b></span><span>ETA: <b>${done?'00:00:00':duration(p.eta_sec)}</b></span><span>Rate: <b>${fmt(p.rate_per_sec,1)} sims/s</b></span></div><p>${r.error?esc(r.error):done?'Run finished.':'execlab is processing the selected parameter grid and intervals.'}</p><p class="muted">The previous successful result remains available until this attempt completes successfully.</p>${done?`<button class="primary" onclick="${r.status==='completed'?`viewResult('${current}')`:'home()'}">${r.status==='completed'?'View Performance':'Back to Scenarios'}</button>`:''}`;updateRunLog(r.log_tail);if(!done)setTimeout(pollRun,800)}catch(e){fail(e)}}
async function cancelRun(){if(confirm('Cancel this run?')){try{await api(`/api/scenarios/${current}/cancel`,{method:'POST'});pollRun()}catch(e){fail(e)}}}
async function viewResult(id){current=id;app.innerHTML='<div class="empty">Loading latest successful result…</div>';try{[currentScenario,perf]=await Promise.all([api(`/api/scenarios/${id}`),api(`/api/scenarios/${id}/performance`)]);activeStrategy=0;selectedStrategies=new Set([0]);strategyPage=1;strategyPageSize=5;intervalPage=1;intervalPageSize=5;performanceScreen()}catch(e){app.innerHTML=`<div class="error">${esc(e.message)}</div><button onclick="home()">← Saved Scenarios</button>`}}
const fmt=(v,d=3)=>v==null||!Number.isFinite(Number(v))?'—':Number(v).toFixed(d);const costClass=v=>v==null?'':v<0?'good':'bad';
const feeFmt=v=>v==null||!Number.isFinite(Number(v))?'—':Math.round(Number(v)).toLocaleString('en-US').replaceAll(',','_');
const wholeFmt=v=>v==null||!Number.isFinite(Number(v))?'—':Math.round(Number(v)).toLocaleString('en-US').replaceAll(',','_');
const priceFmt=v=>v==null||!Number.isFinite(Number(v))?'—':Number(v).toLocaleString('en-US',{useGrouping:false,maximumFractionDigits:4});
function cmpValues(a,b){const an=a==null||(typeof a==='number'&&Number.isNaN(a)),bn=b==null||(typeof b==='number'&&Number.isNaN(b));if(an||bn)return an===bn?0:an?1:-1;if(typeof a==='number'&&typeof b==='number')return a-b;if(typeof a==='boolean'&&typeof b==='boolean')return Number(a)-Number(b);const text=v=>typeof v==='object'?JSON.stringify(v):String(v);return text(a).localeCompare(text(b),undefined,{numeric:true,sensitivity:'base'})}
function sortMark(state,key){return state.key===key?(state.dir==='asc'?' ▲':' ▼'):''}
function toggleSort(state,key){if(state.key===key)state.dir=state.dir==='asc'?'desc':'asc';else{state.key=key;state.dir='asc'}}
const strategyKeys=['index','configuration','mean_is_pct','p15_is_pct','p50_is_pct','p90_is_pct','completion_pct','avg_fee','avg_trades','avg_percent_filled','avg_mean_divergence','avg_max_divergence','status_pct'];
const intervalKeys=['index',null,'session_id','session_start_ts','arrival_mid_price','avg_filled_price','implementation_shortfall_pct','completion_pct','fee','mean_divergence_score','max_divergence_score','num_orders','num_trades','percent_filled','status_ok'];
function strategyValue(v,i,key){if(key==='index')return i;if(key==='configuration')return strategyName(v);return v.stats[key]}
function tablePager(kind,page,size,pages,total){return `<div class="pager"><span>Rows:</span><select onchange="setSummaryPageSize('${kind}',this.value)">${[5,10,20,50].map(v=>`<option value="${v}" ${size===v?'selected':''}>${v}</option>`).join('')}</select><button onclick="moveSummaryPage('${kind}',-1)">&lt;&lt;&lt;</button><input type="number" min="1" max="${pages}" value="${page}" onchange="jumpSummaryPage('${kind}',this.value)"><span>/ ${pages} (${total})</span><button onclick="moveSummaryPage('${kind}',1)">&gt;&gt;&gt;</button></div>`}
function strategySelectionControls(){return `<div class="pager"><b>Selected ${selectedStrategies.size} / ${perf.strategies.length}</b><button onclick="setAllStrategies(true)">Select All</button><button onclick="setAllStrategies(false)">Deselect All</button></div>`}
function setAllStrategies(select){selectedStrategies=select?new Set(perf.strategies.map((_,i)=>i)):new Set();renderStrategyRows();drawStrategyCharts()}
function setSummaryPageSize(kind,value){const size=Number(value);if(kind==='strategy'){strategyPageSize=size;strategyPage=1;renderStrategyRows()}else{intervalPageSize=size;intervalPage=1;renderIntervals()}}
function moveSummaryPage(kind,delta){if(kind==='strategy'){strategyPage+=delta;renderStrategyRows()}else{intervalPage+=delta;renderIntervals()}}
function jumpSummaryPage(kind,value){if(kind==='strategy'){strategyPage=Number(value)||1;renderStrategyRows()}else{intervalPage=Number(value)||1;renderIntervals()}}
function renderStrategyRows(){const body=document.querySelector('.perf section:first-child table tbody');if(!body)return;const ordered=perf.strategies.map((v,i)=>({v,i})).sort((a,b)=>{const order=cmpValues(strategyValue(a.v,a.i,strategySort.key),strategyValue(b.v,b.i,strategySort.key));return strategySort.dir==='asc'?order:-order}),pages=Math.max(1,Math.ceil(ordered.length/strategyPageSize));strategyPage=Math.max(1,Math.min(strategyPage,pages));const start=(strategyPage-1)*strategyPageSize;body.innerHTML=ordered.slice(start,start+strategyPageSize).map((x,j)=>strategyRow(x.v,x.i,start+j+1)).join('');const selection=document.querySelector('#strategy-selection-controls'),controls=document.querySelector('#strategy-page-controls');if(selection)selection.innerHTML=strategySelectionControls();if(controls)controls.innerHTML=tablePager('strategy',strategyPage,strategyPageSize,pages,ordered.length);wireSortHeaders()}
function wireSortHeaders(){const tables=document.querySelectorAll('.perf table'),strategyFeeHeader=tables[0]?.querySelectorAll('th')[7];if(strategyFeeHeader&&!strategyFeeHeader.dataset.sortReady)strategyFeeHeader.textContent='Avg Fees$';[strategyKeys,intervalKeys].forEach((keys,tableIndex)=>{const table=tables[tableIndex];if(!table)return;const state=tableIndex?intervalSort:strategySort;table.querySelectorAll('th').forEach((th,i)=>{const key=keys[i];if(!key||th.dataset.sortReady)return;th.dataset.sortReady='1';th.dataset.base=th.textContent;th.onclick=()=>{toggleSort(state,key);if(tableIndex){intervalPage=1;renderIntervals()}else{strategyPage=1;renderStrategyRows()}}});table.querySelectorAll('th').forEach((th,i)=>{if(keys[i])th.textContent=th.dataset.base+sortMark(state,keys[i])})})}
function performanceScreen(){const s=currentScenario,context=s?`<div class="order-context"><span><small>SYMBOL</small><strong>${esc(s.order.symbol)}</strong></span><span class="side-${esc(s.order.side)}"><small>SIDE</small><strong>${esc(s.order.side.toUpperCase())}</strong></span><span><small>VOLUME</small><strong>${s.order.quantity.toLocaleString()}</strong></span></div>`:'';app.innerHTML=steps(5)+`<div class="row spread"><div><div class="title">PERFORMANCE SUMMARY</div>${context}</div><div class="row"><button onclick="editScenario('${current}')">Modify Setup</button><button onclick="runExisting('${current}')">Run Again</button><button onclick="saveDetails()">Save Scenario Details</button><button onclick="home()">Saved Scenarios</button></div></div><div class="perf"><section><div class="panel"><div class="row spread"><b>STRATEGY COMPARISON</b><div id="strategy-selection-controls"></div><div id="strategy-page-controls"></div></div><p class="muted">Click rows to select/unselect chart lines. Last clicked strategy drives Interval Explorer.</p><div class="scroll"><table><thead><tr><th>#</th><th>Configuration</th><th>Mean IS%</th><th>P15 IS%</th><th>P50 IS%</th><th>P90 IS%</th><th>Completion%</th><th>Fees$</th><th>Avg #Trades</th><th>Avg %Filled</th><th>Avg Mean Div.</th><th>Avg Max Div.</th><th>Status</th></tr></thead><tbody></tbody></table></div></div><div class="stack">${chartBox('is-chart','Implementation Shortfall %','IS% measures total execution performance versus the arrival mid-price, normalized by the initial order value. Lower is better; a negative value means the execution beat the arrival price.')}${chartBox('completion-chart','Completion %','The higher the better.')}${chartBox('filled-chart','%Filled','Trades divided by submitted orders for each interval.')}${chartBox('fee-chart','Fees','Total fee recorded for each interval.')}${chartBox('div-chart','Mean Divergence Score','Effective spread divided by normal spread.')}</div></section><section><div class="panel"><div class="row spread"><div><b>INTERVAL EXPLORER</b><div class="muted" id="active-label"></div></div><div id="page-controls"></div></div><div class="scroll"><table><thead><tr><th>#</th><th>Replay</th><th>Interval</th><th>Start</th><th>IS%</th><th>Completion%</th><th>Fees</th><th>Mean Div.</th><th>Max Div.</th><th>Orders</th><th>Trades</th><th>%Filled</th><th>Status</th></tr></thead><tbody id="interval-body"></tbody></table></div><div id="replay"></div></div></section></div>`;renderStrategyRows();renderIntervals();drawStrategyCharts()}
function clockTime(seconds){seconds=Math.max(0,Math.trunc(Number(seconds)||0));const h=Math.floor(seconds/3600)%24,m=Math.floor(seconds%3600/60),s=seconds%60;return [h,m,s].map(v=>String(v).padStart(2,'0')).join(':')}
function strategyParamsLabel(p){if(p?.start_time_seconds!=null)return `TWAP · ${clockTime(p.start_time_seconds)} · ${fmt(p.time_taken_seconds,0)}s window · every ${fmt(p.trade_frequency_seconds,2)}s`;return `TWAP · ${fmt(Number(p?.elapse_ns)/1e9,2)}s · ${fmt(p?.slice_qty,0)} shares`}
function strategyName(v){return strategyParamsLabel(v.params)}
function strategyRow(v,i,rank=i+1){const classes=[selectedStrategies.has(i)?'selected':'',activeStrategy===i?'active-strategy':''].filter(Boolean).join(' ');return `<tr id="strategy-${i}" data-strategy="${i}" class="${classes}" onclick="toggleStrategy(${i})"><td>${rank}</td><td>${strategyName(v)}</td><td class="${costClass(v.stats.mean_is_pct)}">${fmt(v.stats.mean_is_pct)}</td><td class="${costClass(v.stats.p15_is_pct)}">${fmt(v.stats.p15_is_pct)}</td><td class="${costClass(v.stats.p50_is_pct)}">${fmt(v.stats.p50_is_pct)}</td><td class="${costClass(v.stats.p90_is_pct)}">${fmt(v.stats.p90_is_pct)}</td><td>${fmt(v.stats.completion_pct,1)}</td><td>${feeFmt(v.stats.avg_fee)}</td><td>${fmt(v.stats.avg_trades,2)}</td><td>${fmt(v.stats.avg_percent_filled,1)}%</td><td>${fmt(v.stats.avg_mean_divergence)}</td><td>${fmt(v.stats.avg_max_divergence)}</td><td class="${v.stats.status_pct>=95?'good':'bad'}">${fmt(v.stats.status_pct,1)}%</td></tr>`}
function toggleStrategy(i){activeStrategy=i;if(selectedStrategies.has(i))selectedStrategies.delete(i);else selectedStrategies.add(i);intervalPage=1;renderStrategyRows();renderIntervals();drawStrategyCharts()}
function intervalValue(item,key){const r=item.r;if(key==='index')return item.original;if(key==='session_start_ts')return r.session_start_ts||r.session_ts;return r[key]}
function renderIntervals(){const v=perf.strategies[activeStrategy],size=intervalPageSize,ordered=v.intervals.map((r,original)=>({r,original})).sort((a,b)=>{const order=cmpValues(intervalValue(a,intervalSort.key),intervalValue(b,intervalSort.key));return intervalSort.dir==='asc'?order:-order}),pages=Math.max(1,Math.ceil(ordered.length/size));intervalPage=Math.max(1,Math.min(intervalPage,pages));const rows=ordered.slice((intervalPage-1)*size,intervalPage*size);document.querySelector('#active-label').textContent=strategyName(v);document.querySelector('#interval-body').innerHTML=rows.map((item,j)=>{const r=item.r;return `<tr><td>${(intervalPage-1)*size+j+1}</td><td><button class="primary" onclick="event.stopPropagation();loadReplay('${esc(r.session_id)}','${esc(r.symbol)}')">Replay</button></td><td>${esc(r.session_id)}</td><td>${ictTime.format(new Date((r.session_start_ts||r.session_ts)/1e6))+' ICT'}</td><td class="${costClass(r.implementation_shortfall_pct)}">${fmt(r.implementation_shortfall_pct)}</td><td>${fmt(r.completion_pct,1)}</td><td>${fmt(r.fee,4)}</td><td>${fmt(r.mean_divergence_score)}</td><td>${fmt(r.max_divergence_score)}</td><td>${r.num_orders}</td><td>${r.num_trades}</td><td>${fmt(r.percent_filled,1)}%</td><td class="${r.status_ok?'good':'bad'}">${r.status_ok?'OK':'FAIL'}</td></tr>`}).join('');document.querySelector('#page-controls').innerHTML=tablePager('interval',intervalPage,intervalPageSize,pages,ordered.length);document.querySelector('#replay').innerHTML='';wireSortHeaders()}
const renderIntervalsWithRawFees=renderIntervals;
function ensurePriceColumns(){const row=document.querySelector('.perf section:last-child thead tr');if(!row||row.querySelector('[data-arrival-column]'))return;const reference=row.children[4],arrival=document.createElement('th'),average=document.createElement('th');arrival.dataset.arrivalColumn='1';arrival.textContent='Arrival Mid Price';average.dataset.averageFillColumn='1';average.textContent='Avg Filled Price';row.insertBefore(arrival,reference);row.insertBefore(average,reference)}
renderIntervals=function(){ensurePriceColumns();renderIntervalsWithRawFees();const bySession=new Map(perf.strategies[activeStrategy].intervals.map(interval=>[interval.session_id,interval]));document.querySelectorAll('#interval-body tr').forEach(row=>{const interval=bySession.get(row.children[2]?.textContent),reference=row.children[4],arrivalCell=document.createElement('td'),averageCell=document.createElement('td');arrivalCell.textContent=priceFmt(interval?.arrival_mid_price);averageCell.textContent=priceFmt(interval?.avg_filled_price);row.insertBefore(arrivalCell,reference);row.insertBefore(averageCell,reference);const feeCell=row.children[8],value=Number(feeCell?.textContent);if(feeCell&&Number.isFinite(value))feeCell.textContent=feeFmt(value)});const feeHead=document.querySelector('.perf section:last-child thead tr')?.children[8];if(feeHead){feeHead.dataset.base='Fees$';feeHead.textContent='Fees$'+sortMark(intervalSort,'fee')}};
function chartBox(id,title,note){const descriptions={'Completion %':'Completion% measures how much of the target position was executed by the end of the interval. Higher is better; 100% means the target was fully completed.','%Filled':'%Filled is the number of trades divided by the number of submitted orders. Higher means more submitted orders produced trades; it is not quantity completion.','Fees':'Fees show the total transaction fees recorded in each interval. Lower is better when comparing strategies with similar executed volume.','Mean Divergence Score':'Mean Divergence Score measures simulated order-book impact as effective spread divided by normal spread. A score of 1 means no spread impact; lower is better.'};note=descriptions[title]||note;return `<div class="panel"><div class="chart-head"><div><div class="chart-title">${title}</div><div class="chart-note">${note}</div></div><div class="chart-controls"><b>TIME ZOOM:</b><button data-chart="${id}" data-zoom="in">+</button><button data-chart="${id}" data-zoom="out">−</button><button data-chart="${id}" data-zoom="reset">Reset</button></div></div><div class="legend" id="${id}-legend"></div><canvas id="${id}"></canvas></div>`}
const colors=['#6d3bc1','#16813b','#d36b22','#2563a8','#ba2c36','#008b8b','#a05a00'];
const strategyColors=new Map();
function strategyColor(i){return strategyColors.get(i)||colors[i%colors.length]}
function changeStrategyColor(i){const color=`hsl(${Math.floor(Math.random()*360)} 72% 40%)`;strategyColors.set(i,color);chartInstances.forEach(state=>{if(state.group==='strategy-time')state.series.forEach(s=>{if(s.strategyIndex===i)s.color=color})});document.querySelectorAll(`[data-strategy-color="${i}"]`).forEach(e=>e.style.background=color);redrawGroup('strategy-time')}
const chartGroups=new Map(),chartInstances=new Map();
const ictTime=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Bangkok',month:'2-digit',day:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false});
function groupState(name,points){let g=chartGroups.get(name);const xs=points.map(p=>p.x).filter(Number.isFinite);if(!g){const full=xs.length?[Math.min(...xs),Math.max(...xs)]:[0,1];g={full,domain:[...full],hover:null,drag:null,charts:new Set()};chartGroups.set(name,g)}else if(xs.length){g.full=[Math.min(g.full[0],...xs),Math.max(g.full[1],...xs)];g.domain=[...g.full]}return g}
function redrawGroup(name){const g=chartGroups.get(name);if(g)g.charts.forEach(id=>drawInteractive(chartInstances.get(id)))}
function syncDomain(name,domain){const g=chartGroups.get(name);if(!g)return;const span=domain[1]-domain[0],fullSpan=g.full[1]-g.full[0];let left=Math.max(g.full[0],Math.min(domain[0],g.full[1]-span));g.domain=span>=fullSpan?[...g.full]:[left,left+span];redrawGroup(name)}
function zoomGroup(name,factor,center){const g=chartGroups.get(name);if(!g)return;const [a,b]=g.domain,c=center??(a+b)/2,fullSpan=g.full[1]-g.full[0],span=Math.max(Math.max(1,fullSpan/1000),Math.min(fullSpan,(b-a)*factor));let left=c-(c-a)/(b-a||1)*span;syncDomain(name,[left,left+span])}
function mountChart(id,group,series,zero=true){const canvas=document.getElementById(id);if(!canvas)return;const points=series.flatMap(s=>s.points),g=groupState(group,points);g.charts.add(id);const state={id,group,series,zero,canvas};chartInstances.set(id,state);const legend=document.getElementById(id+'-legend');if(legend)legend.innerHTML=series.map(s=>`<span class="legend-item"><i class="legend-dot" ${s.strategyIndex==null?'':`data-strategy-color="${s.strategyIndex}" onclick="changeStrategyColor(${s.strategyIndex})" title="Click to change this strategy color"`} style="background:${s.color};${s.strategyIndex==null?'':'cursor:pointer;outline:1px solid #222;outline-offset:2px'}"></i>${esc(s.label)}</span>`).join('');canvas.onmousemove=e=>{if(g.drag)return;const r=canvas.getBoundingClientRect(),ratio=Math.max(0,Math.min(1,(e.clientX-r.left-70)/(r.width-90)));g.hover=g.domain[0]+ratio*(g.domain[1]-g.domain[0]);redrawGroup(group)};canvas.onmouseleave=()=>{g.hover=null;redrawGroup(group)};canvas.addEventListener('wheel',e=>{e.preventDefault();const r=canvas.getBoundingClientRect(),ratio=Math.max(0,Math.min(1,(e.clientX-r.left-70)/(r.width-90))),center=g.domain[0]+ratio*(g.domain[1]-g.domain[0]);zoomGroup(group,e.deltaY<0?.936:1.064,center)},{passive:false});canvas.onpointerdown=e=>{g.drag={x:e.clientX,domain:[...g.domain],width:Math.max(1,canvas.getBoundingClientRect().width-90)};canvas.setPointerCapture(e.pointerId);canvas.classList.add('dragging')};canvas.onpointermove=e=>{if(!g.drag)return;const span=g.drag.domain[1]-g.drag.domain[0],left=g.drag.domain[0]-(e.clientX-g.drag.x)/g.drag.width*span;g.hover=null;syncDomain(group,[left,left+span])};const stop=e=>{if(!g.drag)return;g.drag=null;canvas.classList.remove('dragging');if(canvas.hasPointerCapture(e.pointerId))canvas.releasePointerCapture(e.pointerId)};canvas.onpointerup=stop;canvas.onpointercancel=stop;document.querySelectorAll(`[data-chart="${id}"]`).forEach(b=>b.onclick=()=>{const action=b.dataset.zoom;if(action==='reset')syncDomain(group,g.full);else zoomGroup(group,action==='in'?.8:1.25)});drawInteractive(state)}
function drawInteractive(state){if(!state)return;const {canvas,series,group,zero}=state,g=chartGroups.get(group),rect=canvas.getBoundingClientRect(),dpr=devicePixelRatio||1;canvas.width=Math.max(1,rect.width*dpr);canvas.height=Math.max(1,rect.height*dpr);const c=canvas.getContext('2d');c.scale(dpr,dpr);const w=rect.width,h=rect.height,p={l:70,r:20,t:16,b:45},visible=series.map(s=>({...s,points:s.points.filter(q=>q.x>=g.domain[0]&&q.x<=g.domain[1])})).filter(s=>s.points.length),all=visible.flatMap(s=>s.points);if(!all.length){c.fillText('No values in selected range',p.l,p.t+20);return}let ymin=Math.min(...all.map(q=>q.y)),ymax=Math.max(...all.map(q=>q.y));if(zero){ymin=Math.min(ymin,0);ymax=Math.max(ymax,0)}const pad=Math.max((ymax-ymin)*.08,Math.abs(ymax||1)*.001);ymin-=pad;ymax+=pad;const x=v=>p.l+(v-g.domain[0])/(g.domain[1]-g.domain[0]||1)*(w-p.l-p.r),y=v=>p.t+(ymax-v)/(ymax-ymin||1)*(h-p.t-p.b);c.font='12px Courier New';c.strokeStyle='#222';c.beginPath();c.moveTo(p.l,p.t);c.lineTo(p.l,h-p.b);c.lineTo(w-p.r,h-p.b);c.stroke();for(let i=0;i<=4;i++){const v=ymin+(ymax-ymin)*i/4,py=y(v);c.strokeStyle='#ddd';c.beginPath();c.moveTo(p.l,py);c.lineTo(w-p.r,py);c.stroke();c.fillStyle='#333';c.fillText(fmt(v),4,py+4)}if(zero&&ymin<=0&&ymax>=0){c.strokeStyle='#777';c.lineWidth=1.4;c.beginPath();c.moveTo(p.l,y(0));c.lineTo(w-p.r,y(0));c.stroke()}visible.forEach(s=>{c.strokeStyle=s.color;c.lineWidth=1.8;c.beginPath();s.points.forEach((q,i)=>i?c.lineTo(x(q.x),y(q.y)):c.moveTo(x(q.x),y(q.y)));c.stroke();s.points.forEach(q=>{c.fillStyle=s.color;c.beginPath();c.arc(x(q.x),y(q.y),3,0,Math.PI*2);c.fill()})});c.fillStyle='#333';c.fillText(ictTime.format(new Date(g.domain[0])),p.l,h-12);const end=ictTime.format(new Date(g.domain[1]));c.fillText(end,w-p.r-c.measureText(end).width,h-12);if(g.hover!==null){const nearest=visible.flatMap(s=>s.points.map(q=>({s,q,d:Math.abs(q.x-g.hover)}))).sort((a,b)=>a.d-b.d)[0];if(nearest){const hx=nearest.q.x,items=visible.map(s=>{const q=s.points.reduce((a,b)=>Math.abs(b.x-hx)<Math.abs(a.x-hx)?b:a,s.points[0]);return {s,q}}).filter(v=>Math.abs(v.q.x-hx)<Math.max(1,(g.domain[1]-g.domain[0])*.02));c.strokeStyle='#777';c.beginPath();c.moveTo(x(hx),p.t);c.lineTo(x(hx),h-p.b);c.stroke();items.forEach(v=>{c.fillStyle=v.s.color;c.beginPath();c.arc(x(v.q.x),y(v.q.y),5,0,Math.PI*2);c.fill()});c.font='bold 14px Courier New';const lines=[ictTime.format(new Date(hx)),...items.map(v=>`${v.s.label}: ${fmt(v.q.y,4)}`)],bw=Math.min(w-p.l-p.r,Math.max(...lines.map(v=>c.measureText(v).width))+18),bh=lines.length*20+10,bx=Math.min(Math.max(p.l,x(hx)-bw/2),w-p.r-bw),by=p.t+4;c.fillStyle='#fff';c.fillRect(bx,by,bw,bh);c.strokeStyle='#222';c.strokeRect(bx,by,bw,bh);lines.forEach((line,i)=>{c.fillStyle=i?'#111':'#6d3bc1';c.fillText(line,bx+9,by+19+i*20)})}}}
function strategySeries(key){return [...selectedStrategies].map(i=>({strategyIndex:i,label:strategyName(perf.strategies[i]),color:strategyColor(i),points:perf.strategies[i].intervals.map(r=>({x:Number(r.session_start_ts||r.session_ts)/1e6,y:Number(r[key])})).filter(q=>Number.isFinite(q.y))})).filter(s=>s.points.length)}
function setupPerfDivider(){const layout=document.querySelector('.perf');if(!layout||layout.querySelector('.perf-divider'))return;const sections=layout.querySelectorAll(':scope > section');if(sections.length!==2)return;const divider=document.createElement('div');divider.className='perf-divider';divider.title='Drag to resize · double-click to reset';layout.insertBefore(divider,sections[1]);const saved=Number(localStorage.getItem('traderPerfStrategyWidth'));if(Number.isFinite(saved)&&saved>=20&&saved<=75)layout.style.setProperty('--strategy-width',saved+'%');let drag=null,frame=null;divider.onpointerdown=e=>{if(matchMedia('(max-width:1050px)').matches)return;const rect=layout.getBoundingClientRect();drag={x:e.clientX,width:sections[0].getBoundingClientRect().width,total:rect.width};divider.setPointerCapture(e.pointerId);divider.classList.add('dragging');document.body.style.userSelect='none'};divider.onpointermove=e=>{if(!drag)return;const width=Math.max(320,Math.min(drag.total-510,drag.width+e.clientX-drag.x)),pct=width/drag.total*100;layout.style.setProperty('--strategy-width',width+'px');localStorage.setItem('traderPerfStrategyWidth',pct.toFixed(2));if(!frame)frame=requestAnimationFrame(()=>{frame=null;chartInstances.forEach(drawInteractive)})};const stop=e=>{if(!drag)return;drag=null;divider.classList.remove('dragging');document.body.style.userSelect='';if(divider.hasPointerCapture(e.pointerId))divider.releasePointerCapture(e.pointerId);chartInstances.forEach(drawInteractive)};divider.onpointerup=stop;divider.onpointercancel=stop;divider.ondblclick=()=>{layout.style.setProperty('--strategy-width','36%');localStorage.setItem('traderPerfStrategyWidth','36');chartInstances.forEach(drawInteractive)}}
function drawStrategyCharts(){setupPerfDivider();chartGroups.delete('strategy-time');['is-chart','completion-chart','filled-chart','fee-chart','div-chart'].forEach(id=>chartInstances.delete(id));mountChart('is-chart','strategy-time',strategySeries('implementation_shortfall_pct'));mountChart('completion-chart','strategy-time',strategySeries('completion_pct'));mountChart('filled-chart','strategy-time',strategySeries('percent_filled'));mountChart('fee-chart','strategy-time',strategySeries('fee'));mountChart('div-chart','strategy-time',strategySeries('mean_divergence_score'))}
async function loadReplay(session,symbol){const v=perf.strategies[activeStrategy],box=document.querySelector('#replay');box.innerHTML='<div class="replay empty">Loading replay…</div>';const q=`scenario=${encodeURIComponent(current)}&hash=${encodeURIComponent(v.hash)}&session=${encodeURIComponent(session)}&symbol=${encodeURIComponent(symbol)}`;try{const r=await api('/api/replay?'+q);renderReplay(r,session)}catch(e){box.innerHTML=`<div class="replay"><div class="error">${esc(e.message)}</div><button class="primary" onclick="generateReplay('${q}','${esc(session)}')">Replay Anyway</button></div>`}}
async function generateReplay(q,session){const box=document.querySelector('#replay');box.innerHTML='<div class="replay empty">Generating deterministic replay…</div>';try{await api('/api/replay?'+q,{method:'POST'});const r=await api('/api/replay?'+q);renderReplay(r,session)}catch(e){box.innerHTML=`<div class="replay error">${esc(e.message)}</div>`}}
const fillColumns=[['_index','#'],['ts','Timestamp'],['ts_local','Local Timestamp'],['order_id','Order ID'],['side','Side'],['px','Price'],['qty','Quantity'],['maker','Maker'],['position','Position'],['balance','Balance'],['fee','Fee']];
const curveColumns=[['_index','#'],['ts','Timestamp'],['bid','Bid'],['ask','Ask'],['bids','Top 3 Bids'],['asks','Top 3 Asks'],['effective_bid','Effective Bid'],['effective_ask','Effective Ask'],['effective_bids','Effective Top 3 Bids'],['effective_asks','Effective Top 3 Asks'],['my_bid','My Bid (Only Active)'],['my_ask','My Ask (Only Active)'],['position','Position'],['balance','Balance'],['fee','Fee'],['equity','Equity']];
function replayTime(value){const n=Number(value);return Number.isFinite(n)?ictTime.format(new Date(n/1e6))+' ICT':'—'}
function replayLevels(value){return Array.isArray(value)&&value.length?value.map(v=>`${wholeFmt(v.px)} × ${wholeFmt(v.qty)}`).join(' | '):'—'}
function replayCell(kind,key,value){if(key==='_index'||key==='order_id'){const n=Number(value);return Number.isFinite(n)?String(Math.trunc(n)):'—'}if(key==='ts'||key==='ts_local')return replayTime(value);if(['bids','asks','effective_bids','effective_asks'].includes(key))return replayLevels(value);if(key==='maker')return value?'YES':'NO';if(key==='side')return esc(String(value??'—').toUpperCase());if(key==='fee')return feeFmt(value);if(['px','qty','position','balance','equity','bid','ask','effective_bid','effective_ask','my_bid','my_ask'].includes(key))return wholeFmt(value);if(typeof value==='number')return fmt(value,4);return esc(value??'—')}
function replayTableMarkup(kind){const label=kind==='fills'?'FILLS':'CURVE',columns=kind==='fills'?fillColumns:curveColumns,state=replayTables[kind];return `<div class="replay-table-box"><div class="replay-table-head"><h3>${label}</h3><div class="pager"><span>Rows:</span><select onchange="setReplaySize('${kind}',this.value)">${[5,10,20,50].map(v=>`<option ${state.size===v?'selected':''}>${v}</option>`).join('')}</select><button onclick="moveReplayPage('${kind}',-1)">&lt;&lt;&lt;</button><input id="${kind}-page" type="number" min="1" value="1" onchange="jumpReplayPage('${kind}',this.value)"><span id="${kind}-pages"></span><button onclick="moveReplayPage('${kind}',1)">&gt;&gt;&gt;</button></div></div><div class="scroll"><table class="replay-table"><thead><tr>${columns.map(([key,name])=>`<th data-replay-kind="${kind}" data-replay-sort="${key}" onclick="sortReplay('${kind}','${key}')">${name}</th>`).join('')}</tr></thead><tbody id="${kind}-rows"></tbody></table></div><div id="${kind}-empty" class="empty hidden">No ${kind} recorded.</div></div>`}
function renderReplayTable(kind){const state=replayTables[kind],columns=kind==='fills'?fillColumns:curveColumns,source=(replayData[kind]||[]).map((row,index)=>({...row,_index:index+1}));source.sort((a,b)=>{const order=cmpValues(a[state.sort],b[state.sort]);return state.dir==='asc'?order:-order});const pages=Math.max(1,Math.ceil(source.length/state.size));state.page=Math.max(1,Math.min(state.page,pages));const rows=source.slice((state.page-1)*state.size,state.page*state.size),body=document.getElementById(kind+'-rows');if(!body)return;body.innerHTML=rows.map(row=>`<tr>${columns.map(([key])=>`<td>${replayCell(kind,key,row[key])}</td>`).join('')}</tr>`).join('');document.getElementById(kind+'-empty').classList.toggle('hidden',source.length>0);document.getElementById(kind+'-page').value=state.page;document.getElementById(kind+'-page').max=pages;document.getElementById(kind+'-pages').textContent=`/ ${pages} (${source.length} rows)`;document.querySelectorAll(`th[data-replay-kind="${kind}"]`).forEach(th=>{th.classList.toggle('sort-asc',th.dataset.replaySort===state.sort&&state.dir==='asc');th.classList.toggle('sort-desc',th.dataset.replaySort===state.sort&&state.dir==='desc')})}
function renderReplayTables(){renderReplayTable('fills');renderReplayTable('curve')}
function setReplaySize(kind,value){replayTables[kind].size=Number(value);replayTables[kind].page=1;renderReplayTable(kind)}
function moveReplayPage(kind,delta){replayTables[kind].page+=delta;renderReplayTable(kind)}
function jumpReplayPage(kind,value){replayTables[kind].page=Number(value)||1;renderReplayTable(kind)}
function sortReplay(kind,key){const state=replayTables[kind];if(state.sort===key)state.dir=state.dir==='asc'?'desc':'asc';else{state.sort=key;state.dir='asc'}state.page=1;renderReplayTable(kind)}
function orderbookMarkup(){return `<div id="orderbook-playback" class="panel"><div class="chart-head"><div><div class="chart-title">ORDER BOOK PLAYBACK</div><div class="chart-note">Top 3 bid and ask levels. Frames are discrete replay samples; no interpolation.</div></div><span id="ob-time" class="ob-time"></span></div><div class="ob-controls"><button id="ob-prev" onclick="stopOrderbook();renderOrderbook(orderbookIndex-1)">◀</button><button id="ob-play" onclick="orderbookTimer?stopOrderbook():startOrderbook()">Play</button><button id="ob-next" onclick="stopOrderbook();renderOrderbook(orderbookIndex+1)">▶</button><select id="ob-speed" onchange="if(orderbookTimer)startOrderbook()"><option value="0.25">0.25×</option><option value="0.5">0.5×</option><option value="1" selected>1×</option><option value="2">2×</option><option value="5">5×</option></select><input id="ob-slider" type="range" min="0" value="0" oninput="stopOrderbook();renderOrderbook(Number(this.value))"><span id="ob-frame"></span></div><div class="ob-books"><div class="ob-book"><h3>NORMAL BOOK</h3><div class="ob-head"><span class="ob-bid-label">BID QTY</span><span class="ob-bid-label">BID PRICE</span><span class="ob-ask-label">ASK PRICE</span><span class="ob-ask-label">ASK QTY</span></div><div id="ob-normal"></div></div><div class="ob-book"><h3>EFFECTIVE BOOK</h3><div class="ob-head"><span class="ob-bid-label">BID QTY</span><span class="ob-bid-label">BID PRICE</span><span class="ob-ask-label">ASK PRICE</span><span class="ob-ask-label">ASK QTY</span></div><div id="ob-effective"></div></div></div><div class="ob-legend">Blue = bid liquidity · Orange = ask liquidity · bar width is relative quantity within the selected frame.</div></div>`}
function bookLevels(point,key){return Array.isArray(point?.[key])?point[key].filter(level=>Number.isFinite(Number(level.px))&&Number.isFinite(Number(level.qty))).slice(0,3):[]}
function bookFmt(value){return Number(value).toLocaleString('en-US',{useGrouping:false,maximumFractionDigits:4})}
function renderBookRows(target,bids,asks,maxQty){const html=[];for(let i=0;i<3;i++){const bid=bids[i],ask=asks[i],side=(level,kind)=>level?`<div class="ob-side ob-${kind}"><div class="ob-bar" style="width:${Math.max(2,Number(level.qty)/maxQty*100)}%"></div><div class="ob-qty">${bookFmt(level.qty)}</div></div>`:'<div class="ob-empty">—</div>';html.push(`<div class="ob-row">${side(bid,'bid')}<span class="ob-bid-price">${bid?bookFmt(bid.px):'—'}</span><span class="ob-ask-price">${ask?bookFmt(ask.px):'—'}</span>${side(ask,'ask')}</div>`)}target.innerHTML=html.join('')}
function renderOrderbook(index,syncHover=true){if(!orderbookFrames.length)return;orderbookIndex=Math.max(0,Math.min(orderbookFrames.length-1,index));const point=orderbookFrames[orderbookIndex],bids=bookLevels(point,'bids'),asks=bookLevels(point,'asks'),effectiveBids=bookLevels(point,'effective_bids'),effectiveAsks=bookLevels(point,'effective_asks'),qty=[...bids,...asks,...effectiveBids,...effectiveAsks].map(v=>Number(v.qty)).filter(Number.isFinite),maxQty=Math.max(1,...qty);renderBookRows(document.querySelector('#ob-normal'),bids,asks,maxQty);renderBookRows(document.querySelector('#ob-effective'),effectiveBids,effectiveAsks,maxQty);document.querySelector('#ob-time').textContent=replayTime(point.ts);document.querySelector('#ob-slider').value=orderbookIndex;document.querySelector('#ob-frame').textContent=`${orderbookIndex+1} / ${orderbookFrames.length}`;document.querySelector('#ob-prev').disabled=orderbookIndex===0;document.querySelector('#ob-next').disabled=orderbookIndex===orderbookFrames.length-1;if(syncHover){const group=chartGroups.get('replay-curve');if(group){group.hover=point.time_ms;redrawGroup('replay-curve')}}}
function nearestOrderbookIndex(time){if(!orderbookFrames.length||time==null)return orderbookIndex;let low=0,high=orderbookFrames.length-1;while(low<high){const mid=Math.floor((low+high)/2);if(orderbookFrames[mid].time_ms<time)low=mid+1;else high=mid}if(low>0&&Math.abs(orderbookFrames[low-1].time_ms-time)<=Math.abs(orderbookFrames[low].time_ms-time))return low-1;return low}
function stopOrderbook(){if(orderbookTimer){clearInterval(orderbookTimer);orderbookTimer=null}const button=document.querySelector('#ob-play');if(button)button.textContent='Play'}
function startOrderbook(){stopOrderbook();if(!orderbookFrames.length)return;document.querySelector('#ob-play').textContent='Pause';const speed=Number(document.querySelector('#ob-speed').value)||1;orderbookTimer=setInterval(()=>{if(orderbookIndex>=orderbookFrames.length-1){stopOrderbook();return}renderOrderbook(orderbookIndex+1)},Math.max(40,400/speed))}
function initOrderbook(curve){stopOrderbook();const section=document.querySelector('#orderbook-playback'),fillChart=document.querySelector('.replay-fill');if(section&&fillChart)fillChart.after(section);orderbookFrames=curve.map(v=>({...v,time_ms:Number(v.ts)/1e6})).filter(v=>Number.isFinite(v.time_ms)&&(bookLevels(v,'bids').length||bookLevels(v,'asks').length||bookLevels(v,'effective_bids').length||bookLevels(v,'effective_asks').length)).sort((a,b)=>a.time_ms-b.time_ms);if(!orderbookFrames.length){section.innerHTML='<div class="empty">No top-3 order-book samples recorded.</div>';return}orderbookIndex=0;document.querySelector('#ob-slider').max=orderbookFrames.length-1;renderOrderbook(0,false)}
function replaySeries(data,key,label,color){return {label,color,points:data.map(v=>({x:Number(v.ts)/1e6,y:Number(v[key])})).filter(q=>Number.isFinite(q.x)&&Number.isFinite(q.y))}}
function renderReplay(r,session){stopOrderbook();const box=document.querySelector('#replay'),fills=r.fills||[],curve=r.curve||[];replayData={fills,curve};Object.values(replayTables).forEach(state=>{state.page=1;state.sort='ts';state.dir='asc'});box.innerHTML=`<div class="replay"><div class="row spread"><div><div class="title">SESSION REPLAY · ${esc(session)}</div><div class="muted">${fills.length} fills · ${curve.length} curve samples · verification ${r.verify?.matches?'OK':'CHECK'}</div></div><button onclick="stopOrderbook();document.querySelector('#replay').innerHTML=''">Collapse Replay</button></div>${replayTableMarkup('fills')}${replayTableMarkup('curve')}${orderbookMarkup()}<div class="replay-fill">${chartBox('fill-replay','FILLED PRICE','Executed fill price through time (ICT).')}</div><div class="replay-grid">${chartBox('book-replay','BID / ASK PRICE','Normal best bid and ask.')}${chartBox('effective-replay','EFFECTIVE BID / ASK PRICE','Effective best bid and ask after simulated impact.')}${chartBox('position-replay','POSITION','Inventory through the session.')}${chartBox('balance-replay','BALANCE','Cash balance through the session.')}${chartBox('fee-replay','FEE','Accumulated fees.')}${chartBox('equity-replay','EQUITY','Marked equity through the session.')}</div><details class="panel"><summary>Replay data preview</summary><pre>${esc(JSON.stringify({basic:{alpha:r.alpha,hash:r.hash,market:r.market,session_id:r.session_id},verify:r.verify,fills:fills.slice(0,5),curve:curve.slice(0,5)},null,2))}</pre></details></div>`;renderReplayTables();chartGroups.delete('replay-fills');chartGroups.delete('replay-curve');['fill-replay','book-replay','effective-replay','position-replay','balance-replay','fee-replay','equity-replay'].forEach(id=>chartInstances.delete(id));const buys=fills.filter(v=>v.side==='buy'),sells=fills.filter(v=>v.side!=='buy');mountChart('fill-replay','replay-fills',[replaySeries(buys,'px','Buy Fill','#0072b2'),replaySeries(sells,'px','Sell Fill','#d55e00')].filter(s=>s.points.length),false);mountChart('book-replay','replay-curve',[replaySeries(curve,'bid','Bid','#0072b2'),replaySeries(curve,'ask','Ask','#d55e00')],false);mountChart('effective-replay','replay-curve',[replaySeries(curve,'effective_bid','Effective Bid','#0072b2'),replaySeries(curve,'effective_ask','Effective Ask','#d55e00')],false);mountChart('position-replay','replay-curve',[replaySeries(curve,'position','Position','#6d3bc1')]);mountChart('balance-replay','replay-curve',[replaySeries(curve,'balance','Balance','#16813b')]);mountChart('fee-replay','replay-curve',[replaySeries(curve,'fee','Fee','#ba2c36')]);mountChart('equity-replay','replay-curve',[replaySeries(curve,'equity','Equity','#2563a8')]);initOrderbook(curve);['book-replay','effective-replay','position-replay','balance-replay','fee-replay','equity-replay'].forEach(id=>document.querySelector('#'+id)?.addEventListener('mousemove',()=>{const group=chartGroups.get('replay-curve');if(group?.hover!=null)renderOrderbook(nearestOrderbookIndex(group.hover),false)}))}
async function saveDetails(){const s=await api('/api/scenarios/'+current),name=prompt('Scenario name',s.name);if(!name)return;const desc=prompt('Description',s.description||'')??s.description,tags=prompt('Tags (comma separated)',(s.tags||[]).join(', '))??'';s.name=name;s.description=desc;s.tags=tags.split(',').map(v=>v.trim()).filter(Boolean);await api('/api/scenarios/'+current,{method:'PUT',body:JSON.stringify(s)});alert('Scenario saved')}
window.addEventListener('resize',()=>chartInstances.forEach(drawInteractive));
applyLanguage();
(async()=>{try{meta=await api('/api/meta');document.querySelector('#env').textContent=`${meta.timeframe} · ${meta.data_dir}`;await home()}catch(e){fail(e)}})();
</script></body></html>"####;
